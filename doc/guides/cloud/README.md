# 云端操作指南

本目录保存 CompShare 等云环境中的可复用操作说明。

- `compshare-filebrowser-legacy.md`：CompShare 厂商预装 File Browser 的兼容与故障恢复；上游已宣布停止后续维护并将归档，不作为无条件推荐方案。
- `openlist-quark-download.md`：通过 OpenList 和夸克驱动执行一次性云端数据取回；夸克驱动是未主动维护的逆向接口。

这些指南不得保存密码、Cookie、Token、带凭据 URL 或实例私密配置。平台版本、端口入口、自启动机制和驱动字段在每个新实例上都要重新核验。
