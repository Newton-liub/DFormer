# MUSeg 阶段二：DFormerv2-S 长程 baseline 与后续 MVE 总索引

> 文档状态：执行中；**01 已完成并冻结，用户门禁 A 已批准；02 已提交为 `d9ef428`；03 已提交为 `e9e3c1c`；01–03 的门禁 B 修复已通过最终无卡复核并签署。04 仍未启动，且必须先由用户开启有卡模式。**
>
> 起点定义：这里的“阶段二”是“建立 DFormerv2-S 长程 baseline”，不是旧交接文档中的 GPU preflight 阶段。
>
> 执行模型：默认由 Terra 按文件顺序执行；标为 **Sol 必须执行/复核** 的任务不得由 Terra 独立决策。01 的协议、实现、测试、真实划分、审计、Sol 复核、用户门禁 A 与冻结发布均已完成。

## 1. 本阶段目标

1. 将历史 20-epoch 链路降级为 qualification（工程资格验证），不再称为正式 baseline。
2. 从 MUSeg 官方 `train.txt=1595` 内按 location group 构建固定 development train/validation；官方 `test.txt=1576` 在开发阶段封存。
3. 建立可恢复、可审计、三随机种子、最长 500 epochs 的 DFormerv2-S 长程训练链路。
4. 仅用 validation 冻结训练周期、checkpoint 选择和正式实验协议。
5. 在成熟 baseline 上重新运行正式 A2；历史 epoch-10/16 图 A2 仅作为 pilot 证据。
6. 只有正式 A2 达到预注册门槛才进入 B2；否则停止 B2 主线。

## 2. 明确不做的事情

- 01 已生成并冻结开发划分；后续阶段不得重新生成、换 seed、手调 membership，或用官方 test 选择 epoch、超参数、seed、A2 条件或 B2 结构。
- 在门禁 B–G 分别批准前，不提前运行对应 GPU qualification、长程训练、test 评估或 B2。
- 不把单次 20 epochs、单个最好 seed 或 test 最优 checkpoint 写成正式 baseline。
- 不重新研究或撤销已经完成的 A1/B1 张量级结论；只补真实模型混合 batch 回归。
- 不默认复现论文中的 DFormer-S 数值。论文协议已经在阶段一核对；本阶段目标模型是 DFormerv2-S。若用户另行要求严格 DFormer-S 数值复现，必须另立计划。

## 3. 全局事实与固定边界

- MUSeg 论文：官方 train/test 为 1595/1576；同一拍摄位置组不得跨 split；所有模型 500 epochs、3 seeds。
- 本地静态核验：官方 train 有 958 个位置组，test 有 957 个位置组，位置组交集为 0。
- 位置组键来自文件名的前四段，例如 `06-01-01-0215-...` 的键是 `06-01-01-0215`。
- 当前代码 `local_configs/MUSeg/DFormerv2_S_4090.py` 将训练期 `eval_source` 指向官方 `test.txt`，必须在长程训练前纠正。
- 当前 `utils/train.py` 只在 epoch 1、每 10 epochs 和 `checkpoint_start_epoch` 之后验证；best、resume、seed 和元数据能力不足，必须先改造。
- 当前 A2 工具只覆盖 16 图、block mask、`q=0/0.3/0.5`、单 seed；B2 尚未实现。
- 当前工作区可能包含计划外的 Cursor agent/workflow 修改。执行者不得覆盖、清理、暂存或提交不属于当前子计划的文件。

## 4. 计划文件与执行顺序

| 顺序 | 文件 | 主要产物 | 模型要求 | 进入条件 |
|---:|---|---|---|---|
| 01 | `01-开发划分协议与生成工具.md` | group-aware split 工具、manifest、哈希和测试 | **Sol 协议/复核已完成；Terra 实现已完成** | **已完成：门禁 A 已批准，冻结产物已发布** |
| 02 | `02-训练验证Checkpoint与恢复改造.md` | 独立 val、验证调度、best/latest、可靠 resume | **Sol 设计与核心复核已完成；实现和 CPU 测试已通过** | **已完成并提交为 `d9ef428`** |
| 03 | `03-三种子编排Preflight与SwanLab改造.md` | seed CLI、元数据、preflight、probe、顺序 launcher、汇总 | Terra 可实现；**Sol 最终复核** | **已完成并提交为 `e9e3c1c`；门禁 B 已签署** |
| 04 | `04-静态验收与4090Qualification.md` | 静态测试、短跑、恢复演练、batch 选择报告 | Terra 执行；异常由 **Sol 判断** | 01–03 全部合入且工作区干净 |
| 05 | `05-开发长程训练与协议冻结.md` | dev train/val 长程曲线、候选 epoch、冻结协议 | Terra 机械运行；**Sol 做全部研究判断** | 04 通过且用户批准长训 |
| 06 | `06-正式三种子Baseline与Test解封.md` | 全 1595 训练的 3 seeds、一次性 test、mean±std | Terra 顺序执行；**Sol/用户控制 test 解封** | 05 协议已冻结 |
| 07 | `07-A2正式筛查工具扩展.md` | 64/全 val-dev、多 q/mask/fill/seed、配对统计工具 | Terra 可实现；**Sol 复核统计协议** | 06 正式 baseline 验收完成 |
| 08 | `08-A2正式运行与B2门禁.md` | 正式 A2 报告和 A2-G/N-G 决策 | Terra 运行；**Sol 解释并签署门禁** | 07 工具验收通过 |
| 09 | `09-B2几何规格与金标准.md` | validity 数据流和两类 GSA 的数学/张量规格 | **仅 Sol；不得交 Terra 独立设计** | 08 判定 A2-G |
| 10 | `10-B2实现与零训练筛选.md` | B2 代码、几何单测、零训练 B0/B2 结果 | Terra 按 09 实现；**Sol 逐行复核核心几何** | 09 已签署 |
| 11 | `11-B2短微调与完整对照.md` | 5-epoch gate、必要时 3-seed 完整 B0/B2 | Terra 运行；**Sol 负责决策与结论** | 10 的 G0 门槛通过 |

任何文件未通过完成标准时，不得提前执行后续文件。

## 5. 全局人工门禁

- **门禁 A：划分冻结。** 用户确认 train-dev/val-dev 数量、位置组零重叠、六矿分布、类别分布和 SHA-256。
- **门禁 B：代码链路冻结，已签署。** 01–03 的冻结 authority 绑定、候选来源证明、三相位 source 闭合、qualification 模板/物化可追溯性、无卡测试与干净提交边界均已通过最终复核；可以在用户明确开启有卡模式后申请 GPU qualification。
- **门禁 C：batch 选择。** 4090 探测后先报告，用户确认正式 batch。
- **门禁 D：开发长训。** qualification 和 resume 演练通过后，用户批准长程 dev 训练。
- **门禁 E：正式协议冻结。** Sol 只根据 validation 曲线提出训练周期和选择规则，用户确认后不再修改。
- **门禁 F：test 解封。** 三个正式 seed 的预定 checkpoint、哈希、退出码全部合格后，用户明确批准 test 评估。
- **门禁 G：B2。** 正式 A2 达到门槛且 Sol 签署 A2-G 后才允许进入 09。

## 6. 每个执行代理的统一规则

1. 开始前读本索引、当前子计划、所有前置完成报告和实际目标文件。
2. 先运行 `git status --short`；发现计划外改动时停止，不得 stash/reset/覆盖。
3. 一个子计划只产生一个边界清晰的代码提交；运行日志、数据、权重、checkpoint、凭据不进 Git。
4. 代码修改前先补或设计失败测试；修改后只运行受影响测试，再运行计划指定的完整测试。
5. 不顺手重构，不更换框架，不创建新 Conda/venv，不改原始数据。
6. API key 只从环境读取，不回显、不写文件。
7. 需要修改计划外文件、改变实验协议或解释异常时停止并升级给 Sol。
8. 执行报告必须包含 commit、命令、退出码、测试结果、产物路径与哈希、未解决风险和 `git status --short`。

## 7. Git 与证据规则

- 计划文件和小型代码/测试可提交；01 最终由用户批准冻结的轻量列表、manifest 和 audit report 应提交，逐图/逐像素缓存与临时候选目录不提交。
- 每个代码阶段单独提交，提交消息说明阶段编号；不得把多个阶段压成一个不可审计提交。
- 正式运行必须使用干净 commit；运行中若修代码，当前 run 作废，修复独立提交后从受影响阶段重来。
- 输出根目录按实验 ID/seed 隔离；禁止多个 run 共用 checkpoint 或预测目录。
- 所有 JSON/CSV 汇总应包含 schema version、完整 Git commit、split SHA-256、checkpoint SHA-256、seed 和生成命令。

## 8. 当前状态

- **01 已完成：** Sol 协议、Terra 工具实现、真实确定性划分、技术复核、用户门禁 A、冻结发布与独立只读审计全部完成。
- **冻结路径：** `data/splits/MUSeg/dev-v1`。train-dev 1277 张/762 groups；val-dev 318 张/196 groups；official test 1576 张/957 groups；train-dev 与 val-dev 精确闭合为 official train，三侧样本/group 两两零交叉。
- **冻结哈希：** `train-dev.txt=a6b15b63f6d5193e3928ea24ada25be403a48e68d1c1f9372cdbbc3fe5cd8470`；`val-dev.txt=1d0719d8f64f016d48995c25ab66d4004d76b7155d9efeef7cbb7454c0dd0e83`；`official-test.txt=12d9834215fcbfe696ad88321539c224850ff6fb66a01f48a02b1df478f48a4b`；`manifest.json=42233412f432e387cfcffc763724461e2dbc111969a595c714ac12add7bf7b01`；`audit-report.json=53ac30aba0230919b994202f37b3571a7b416f9129f27eabf003415721e38055`。
- **审计结果：** 冻结目录的独立只读 audit 为 `pass=true`，102/102 checks 通过；冻结 manifest 记录 `candidate_status=frozen`、`user_gate_a.status=approved`，并以 `source_candidate_manifest_sha256=1fa8fbc1613bad8a5d251cf8949f4dd5e5cd2dc9c82ab3cbf5ee4384f556dd63` 绑定获批候选。
- **Sol 判断：** 三项非硬约束 warning 已解释并由门禁 A 接受；不换 seed、不手调样本、不修改冻结算法或 membership。
- **02 已完成并提交：** 显式 train/val/test source、纯函数验证与保存调度、严格 best、原子 latest/periodic/best checkpoint、版本化 epoch-boundary resume、AMP/RNG/optimizer/global-step/split 协议恢复和不兼容拒绝已提交为 `d9ef428`；Sol 已完成核心代码复核，CPU 测试与完整 `tests/` 套件通过。
- **03 已完成并提交：** 版本化 protocol manifest、单 seed launcher、严格顺序三 seed orchestrator、汇总器、preflight 静态审计、SwanLab 完整元数据和 4090 shell 已提交为 `e9e3c1c`；Sol 已复核字段契约、失败传播、checkpoint/训练结果身份绑定、official test 封存与端到端接口。
- **门禁 B 已签署：** 冻结 authority 修复已独立提交；protocol、preflight、launcher 与汇总器从 `data/splits/MUSeg/dev-v1/manifest.json` 派生并校验 split 身份，候选来源哈希由 canonical 重建测试证明，qualification 使用跟踪的不可运行模板和仅限有卡机器物化的本地 manifest。最终无卡验证、静态检查、干净工作区及正式整体复核均已通过。
- **后续门禁：** 04 及 GPU qualification 仍不得在未开启有卡模式时执行。本次没有运行 GPU 或模型训练，也未在线初始化 SwanLab。原始数据未修改，用户豁免的 `liu-test-exp/方案1/` 未纳入本阶段。
