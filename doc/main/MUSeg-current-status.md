# MUSeg 当前状态与唯一实时入口

> **状态时间：** 2026-09-15（MMFR A2 v3 本地资格、预训练冻结、云端实例创建后立即停机，以及该实例重新启动后数据集落地核验批次）
> **当前阶段：** 状态标识为 `eligible-for-cloud-capacity-probe-authorization`。当前身份已升级为 `MMFR-A2-train-integration-v3`（分析身份 `MMFR-A2-clean-control-v3` 与 `MMFR-A2-depth-corruption-train-v3`）；高级审计已正式选择 **A + MID-A**，即 natural-invalid 监督保留，intensity corruption 不复活 invalid Depth，blur 使用 mask-normalized blur，misalignment 同时搬运 Depth 与 validity，最终 target 为 `V_state_final * R_syn`。v3 所需五项本地资格——severity/burden、CPU qualification、validity transport、initial-state equivalence、AMP update-path isolation——均已形成独立结构化 `PASS` 证据。`MMFR-A2-train-integration-v2` 已标记为 `superseded-before-formal-training-after-validity-semantic-review`，原 commit `3c8ebddeb76e63a5be37e261381117abfba117ca`、annotated tag `MMFR-A2-v2-pretrain-freeze` 及全部历史证据保留。v3 以新的 annotated tag `MMFR-A2-v3-pretrain-freeze` 固定，未覆盖 v2 tag；该批次**当时**未推送远端，推送状态的后续变化见本节 2026-09-15 云端实例条目。
> **授权边界：** `eligible-for-cloud-capacity-probe-authorization` 只表示现在可以向用户申请云端单 GPU、batch size `10` 的容量/吞吐短 probe；它不表示该 probe 已获授权。云实例创建已由用户单独授权并完成（只做创建后立即停机，未登录实例、未传输数据、未运行 probe）；正式 500 epoch 训练、完整 evaluator、checkpoint 效果评价、official test 以及本次创建之外的任何云资源操作均未运行、未授权；official test 继续为 `sealed_unread`，全部 v3 报告均记录 `official_test_included=false`。
> **大白话说明：** 高级审计要求的有效性语义已经按新 v3 身份实现并通过五道本地门禁，代码和证据现在可以冻结。下一步只能先询问你是否允许租用云端单卡做 batch size 10 的短容量/速度测试。用户已单独授权创建云实例，现已按 RTX 4090 / 14 vCPU / 32 GiB 建好并立即关机，等你确认配置无误；登录实例、准备数据、跑 probe 和开始训练都还没有做。
> 本文件是 MUSeg 当前事实、授权边界、证据入口和恢复规则的唯一实时入口；计划、报告、审计和 Canvas 只承担各自形成时点的历史或详细证据职责。

## 1. 稳定基线

- **模型与输入：** `DFormerv2-S RGB Quick-B0`，使用 RGB 输入契约 `rgb-imagenet-rgb-order-v1`；B1 `safe_masked_mean` 数值稳定性修复保留在基线中。
- **最终 checkpoint：** epoch 420 的 `selector-epoch-420.pth`，SHA-256 为 `f246a3afc50334c81302b7bfebdadf7cf37d00326bf1c3aa54f6a151754e3a1c`。
- **开发 split：** `val-dev` 共 318 条，split SHA-256 为 `1d0719d8f64f016d48995c25ab66d4004d76b7155d9efeef7cbb7454c0dd0e83`；official test 仍为 `sealed_unread`（封存未读）。
- **主 evaluator：** `msflip-whole-original-grid-v1`，5 个尺度分别使用原图与水平翻转，共 10 个 view；输出恢复到 MUSeg 原始 Label 网格，以 FP32 平均 pre-softmax logits 后计分。
- **主指标：** mIoU `58.79`、mAcc `69.91`、mF1 `72.73`。
- **基线性质：** 这是 single-seed、RGB、development Quick-B0，作为后续模块探索的固定内部对照；它不是三 seed 的完整论文复现，也不是颜色优劣实验结论。
- **权威证据：** 主评估裁决见 `doc/reports/2026-08-31-museg-quick-b0-main-evaluation.md`；综合报告见 `doc/reports/2026-08-31-museg-dformerv2-quick-baseline-comprehensive.md`；完整本地证据位于仓库外的 `cloud/DFormer-quick-b0-evidence/museg-dformerv2-s-rgb-quick-b0-v1/`（该目录不纳入 Git）。

## 2. 当前研究方向状态

- 2026-09-08 的计划门禁已确认：DFormerv2 的 Depth 在四级 GSA 中形成几何先验；现有置信融合/回退文献依赖专门训练，首轮不能支持冻结 logits 门控；三维评价、严格 RGB-only 回退、亮度引导补全、高置信阈值和可学习 logits 门控均已从首轮删除。`RE447` 不适合作 bootstrap 来源，配对 location-group 重采样采用项目预注册流程；历史 `boundary_band_mIoU` 不等同标准 Boundary IoU。详细裁决保留在本计划目录与 `MUSeg-open-decisions.md`。
- `DVC-A1-valdev-boundary-zero-v1` 的 protocol 模板、专用物化/校验器、确定性 Depth16 corruption、标准 Boundary IoU、location-group bootstrap/裁决和五尺度翻转运行入口已实现于当前未提交工作区；聚焦测试为 `5 passed`，两条真实 `val-dev` 样本的 GPU preflight 已通过 q=0 decoded-array 等价、mask 确定性/嵌套/面积匹配、有限值、strict checkpoint load 和原始 `932×1082` Label 网格恢复。
- 2026-09-08 的完整 mask 门禁已读取 318 条 `val-dev` 的 Depth16 并完成全部 mask 扫描：58/196 个 location group 无法构造非空 `boundary-q75`，比例 `29.5918%`，高于预注册上限 `5%`；非边界 q50 候选不足为 0 条。因此裁决为 `protocol-blocked`，完整 5 condition × 10 view 模型评价没有开始，也没有生成 mIoU、Boundary IoU、bootstrap 区间或 `supported/not-supported/inconclusive` 科学裁决。大白话说，v1 的操作定义覆盖不了足够多的位置，所以按原规则停止；这个历史结论保持不变。
- 2026-09-08 已处置并物化后继研究选择：独立 `DVC-A1-valdev-boundary-zero-v2` 保持全局相对深度跳变阈值 `0.05` 和冻结 `val-dev` 不变，预先纳入至少一张图可构造非空 q75 的全部 138 个 location group，共 218 张图。31 张图在组级纳入后仍无实际 q75 置零，主分析必须保留并显式报告；另对 123 个组内每张图均可构造 q75 的组做预注册敏感性分析。修正前 v2 protocol、allowlist 和两样本本地 GPU preflight 已物化/通过；保护修正后的 protocol 已重新物化，修正后的 preflight 已通过，完整评价随后按批准执行并以 `protocol-blocked` 结束。大白话说，评价范围已固定，正式推理已完成，但统计保护发现一个预注册组的 Boundary IoU 未定义，当前没有模型好坏或问题是否成立的结论。
- 2026-09-08 的最终差异审查已落实：v2 运行入口现在会把新生成的 218 样本、五个 condition 的 mask 哈希逐一比对冻结 v1 manifest，并在形成统计量前强制主分析 138 组和敏感性分析 123 组的两个 effect 都有对应数量的有效配对组。修正后的两样本 preflight 已通过，完整评价实际于 `2026-09-08T14:34:00.225007+00:00` 启动，并于约 90 分 45.565 秒后结束。运行实际写出五个 condition 产物，但 `dose_effect` 的 138 组门禁发现只有 137 个有效配对组，遂以 `protocol-blocked` 结束。只读核对显示无效组为 `06-01-01-0346`；在 123 组敏感性范围内，两个 effect 均为 123 个有效配对组。大白话说，模型推理跑完了，但预注册统计范围与 Boundary IoU 可定义范围仍有一个组不一致，所以当前数值不能作为科学结果。
- **当前授权与恢复点：** v3 主裁决保持 `not-supported`。`DVG-B1-oracle-gsa-v1` 的 A/B/C 和 P0 protocol 保持冻结；P1 CPU qualification、P2 本地 GPU no-op、P3 真实受损 gate preflight 和 P4 218 张图完整本地 GPU paired development evaluation 均已完成。P4 的身份、输入、mask、q=0、finite、原图网格、clean 严格等价和 138 组配对统计门禁全部通过，但主条件 `boundary-q75` 的 Boundary IoU 与 mIoU 均为负，最终裁决为 `oracle-not-supported`。当前恢复点是停止该 Oracle GSA 门控方案，回到方向级候选选择；训练、云资源和 official test 未授权。大白话说，实现和完整评价都已可信完成，但这个理想门控动作本身让结果变差，因此不继续沿这条路线投入。
- 2026-09-10 已完成 B1 的项目内实现锚点核对并收紧目标计划：`GeoPriorGen.forward` 中 spatial contribution 与 depth geometry contribution 可在加和前分离，前三个 stage 使用 H/W 分解 GSA、第四个 stage 使用 Full GSA；最小 Oracle 参数链和唯一 depth-only 插入点已明确，Attention 继续只消费合成后的 geometry prior。现有五尺度翻转、Depth16 corruption、q=0 输入等价和 preflight/JSON 框架可直接复用。当前 `models/encoders/DFormerv2.py` 与作者原始保留副本 `D:/0Project/origin/DFormer/models/encoders/DFormerv2.py` 已再次完整核对：两者 SHA-256 均为 `2b0b77ea401d56993aac915883bcb43035927ec991501dba94fb029901009332`，完整差异检查退出码为 `0`，且都使用 bilinear interpolation；论文文字描述 average pooling。因此该差异属于作者论文与作者代码之间的上游差异，不是 MUSeg/MVE 适配或当前项目意外修改，B1 基线语义以 checkpoint 对应的 bilinear 路径为准。截至该核验时点仍处于 `reference-blocked` 的内容分为三组：A 为像素 corruption mask 到四级 token reliability 的确定性聚合，B 为 token reliability 到 Full/H/W pairwise depth contribution gate 的提升，C 为 `oracle-supported` 最小实际效应量、clean 不劣容忍度和额外指标必要性。A、B 保留 WOS 靶向检索式和待填字段；C 等待用户补充参考或明确项目预注册选择。本次未执行外部 Web 搜索。证据入口为 `04-DVG-B1条件式Oracle门控.md` 与 `liu-test-exp/方案1/DVG-B1-必须实现细节靶向检索步骤与WOS检索式.md`，研究选择见 `MUSeg-open-decisions.md` 第 12 节。代码、preflight、GPU、训练、云资源和 official test 仍未授权。大白话说，现有 GSA 实现已经核清；下一步只补 A、B、C 的规则依据，不写代码也不跑模型。
- 2026-09-11 已只读核对 `doc/paper/补充材料2` 中对应优先清单的 11 篇全文，并把公式、方向性、归一化、hard/continuous、代码 URL 和负证据回填到 `liu-test-exp/方案1/DVG-B1-必须实现细节靶向检索步骤与WOS检索式.md`。A 的最强直接证据来自 *Confidence Propagation through CNNs*：二值 validity 可作为连续 confidence，逐层 confidence 使用 normalized aggregation，多尺度下采样采用 confidence max pooling 与 argmax feature selection；但没有 DFormerv2 四级 kernel/stride/padding、空窗口严格规则或与 `bilinear, align_corners=False` 的共同对齐。B 的直接证据包括 NL-3A 与 GSCS/SPN 的邻居/key-only 连续公式 $w_{p,q}=c_q\hat w_{p,q}$、NR-MVSNet 的 query-only hard threshold，以及 SMAC 的 $HW\times HW$ Full attention 拓扑；这些机制互不等价，也没有任何一篇同时给出 Full/H/W 三种 gate。因此在全文核验完成的该时点，A/B 仍为 `reference-blocked`，恢复点是补官方代码或由用户作项目预注册选择；后续官方代码核验与用户选择见下两条更新。
- 2026-09-11 已继续完成 11 篇论文的官方代码核验并更新同一检索文档：确认 [1]、[3]、[5]、[9]、[10] 和 [11] 存在与论文绑定的作者/官方仓库，但 [11] SMAC 仓库只含 ReDWeb-S 数据集、统计与结果，没有模型代码；作者前作 S2MA 只能作为邻近 Full-affinity 拓扑参考。`D:/0Project/origin` 下 `nconv`、`nconv-nyu`、`LightDepth`、`OPM-MVS`、`NR-MVSNet`、`LFDA`、`SMAC`、`S2MA` 已固定分支、commit、许可证状态和函数锚点，复核时 8 个仓库均无工作区改动。nconv 代码的 `maxpool/4` 与零 padding 不满足 strict all-1 reliability，NR-MVSNet 是 `[B,1,H,W]` query/location hard gate，LFDA 是 candidate attention，S2MA 不是 SMAC 实现；代码仍没有给出统一 Full/H/W gate。两个内联 CPU probe 仅核对 resize/pooling/padding/flip 和 query/key/product/min 的算子语义，未加载模型或 checkpoint。在官方代码核验完成的该时点，A/B 仍为 `reference-blocked`，等待用户决定继续窄搜或作项目预注册选择；后续选择已记录在下一条 03:20 UTC 更新中。该时点仍未创建 B1 protocol，也未修改模型代码或运行模型 forward、preflight、GPU、训练、云资源、official test。
- 2026-09-11 03:20 UTC，用户已选择把 A/B 固定为项目预注册候选，并完成 `04-DVG-B1条件式Oracle门控.md`、新对话入口、总规划、检索证据和开放决策同步。A 采用“原图 reliability `INTER_LINEAR` 到 view → resize 后 flip → 右/下可信 padding → `INTER_AREA` 到四级 token”的连续有效面积比例；B 采用 Full/H/W query/key 对称乘积。clean 旁路严格 `torch.equal`，指标集合固定 Boundary IoU + mIoU；C 的最小实际效应量、95% 区间规则和 mIoU 职责仍待冻结。计划已细化为 P0 protocol、P1 CPU qualification、P2 no-op、P3 1–2 样本 preflight、P4 另行授权完整评价。没有创建 protocol，没有修改模型代码，也没有运行模型 forward、GPU、训练、云资源或 official test。大白话说，该时点下一模型不用再选 A/B，只需先补 C。
- 2026-09-12 17:03 UTC，用户在未查看 DVG-B1 模型结果前冻结 C：Boundary IoU 最小实际净增益 `+0.10` 个百分点，点估计达到门槛且 95% percentile interval 下界严格大于 `0`，mIoU 点估计不得为负。已创建 `protocols/dvg-b1-oracle-gsa-v1.template.json` 与 `tools/mve/dvg_b1_protocol.py`，并将 P0 protocol 物化到仓库外 `cloud/DVG-B1-oracle-gsa-v1/protocol.json`；最终 SHA-256 为 `e7b9ed0a3c84053736f70a7807bdd4f270ee5bf84a6b85ca5cfd053f9cc47e46`，同目录 `allowlist-summary.json` 绑定 218 张图、138 个 location group 及 v3 source identities，`official_test_included=false`。Python 静态编译、身份校验和确定性重物化通过；没有实现 A/B 模型门控，也没有运行模型 forward、preflight、GPU、训练、云资源或 official test。大白话说，P0 已经准备好并锁定规则，但真正的门控代码还没开始，下一步要先单独批准 P1。
- 2026-09-13 03:56 UTC，用户已依次授权并完成 P1 与 P2。P1 首次 CPU qualification 以 `protocol-blocked` 合法暴露三类审计问题：PyTorch area 与冻结 OpenCV `INTER_AREA` 最大差 `1.5497e-06`、W gate expected shape 少 channel 维、以及“inverse-flip 差异只在边缘 token”的假设错误；修正为真实 OpenCV 聚合、正确 W shape、scaled content 严格镜像且每 view/stage 独立核对后通过。model-side 另增加双轴整除 32 和精确 4/8/16/32 stage 几何守卫；最新 qualification SHA-256 为 `e101c972a25ada6749e7d8408172938d104ecfb4eee9266f1fbab7fc632b3dc3`，execution 为 `cloud/DVG-B1-oracle-gsa-v1/executions/20260913T035625995821+0000-qualification.json`。P2 首个结构化尝试中四级 backbone 已全部相等，但 Ham decoder eval 会随机初始化 NMF bases，未配对 RNG 时最终 logits 不同；失败产物已归档为 `cloud/DVG-B1-oracle-gsa-v1/attempts/noop-equivalence-e916e463d64456f2e971b638ff1dc4385dbd13e4b19232b8bb1939558cd18ec0.json`。在每个 forward 前回放同一 CPU/CUDA RNG state 后，四类 no-op 输入的四级输出和最终 logits 全部 `torch.equal`；最终 SHA-256 为 `a9cdbe8ca3b0ef210184499a06702103e71855e8b3529472403aae00fe31df24`，execution 为 `cloud/DVG-B1-oracle-gsa-v1/executions/20260913T035646371110+0000-noop-equivalence.json`。该 P2 使用本地 RTX 5060 Laptop GPU、strict epoch 420 checkpoint 和一个 0.5 scale 非 flip 的 v3 allowlist 样本，`official_test_included=false`。没有运行 P3/P4、训练或云资源。大白话说，接口的无副作用已经实测通过，但真实坏区门控仍未预检。
- 2026-09-13 08:56 UTC，用户单独授权的 P3 本地 GPU gate preflight 已在独立复核后强化重跑并通过。按冻结 v3 mask manifest 中“优先最大化两类受损条件的较小 mask 数量”的确定性规则选中 `02-01-01-0283-240524103235-08-99`；`boundary-q75` 和 `nonboundary-q50` 分别含 `22,368` 与 `14,912` 个受损像素，重建 hash 与冻结 source manifest 完全一致，q=0 Depth8 与生产 Depth8 数组相同。clean、`boundary-q75`、`nonboundary-q50` 各运行 10 个 view，共 30 个 paired baseline/gated view；clean 的 mask support 和 Depth delta 均为 0，10/10 view 和融合后 logits 严格相等。两个受损条件的 20 个 view 均实际改变 Depth，且变化像素全部落在对应缩放/翻转/padding 后的 mask support 内；80 个 baseline 加 80 个 gated 四级首块 GSA 审计均捕获完整 stage/topology、同一 corrupted Depth 输入和有限贡献，baseline 生产 geometry prior 精确等于 spatial contribution 加 raw depth contribution，gated 则精确等于 spatial contribution 加 gated depth contribution，抽样重建误差全部为 `0`，gated reliability/gate 非全 1 且 gate effect 非零。baseline/gated forward 在每对比较前回放同一 CPU/CUDA RNG state，artifact 已显式记录 decoder `rand_init=true`。全部 logits 有限并恢复到原始 `932×1082` Label 网格，两个受损条件的融合 logits 均与 baseline 不同；峰值 CUDA memory 为 `5,704,256,512` bytes，受损条件四级 GPU→CPU→OpenCV→GPU 聚合计时合计 `0.191766` 秒/20 view，仅作 P4 预算描述。最终 canonical `cloud/DVG-B1-oracle-gsa-v1/gate-preflight.json` SHA-256 为 `bd2ec0f677b89a5a3129c1823c876b5b510262f28d8d96499d023af826c1360c`，execution 为 `cloud/DVG-B1-oracle-gsa-v1/executions/20260913T085636085128+0000-gate-preflight.json`；此前 P3 产物均已归档到 `attempts/`，不再是 canonical。`official_test_included=false`，P4、训练和云资源未运行。大白话说，强化后的证据同时证明坏区确实改变了对应 Depth、mask 与变化区域对齐，而且生产基线与门控路径只在冻结的 depth contribution gate 上不同；这仍不能替代 218 张图完整效果评价。
- 2026-09-13 15:05 UTC，用户明确授权的 P4 已在本地 NVIDIA GeForce RTX 5060 Laptop GPU 上完成。冻结范围为 218 张图、138 个 location group、`clean`/`boundary-q25`/`boundary-q50`/`boundary-q75`/`nonboundary-q50` 五个 condition、每图 10 view，共 `10,900` 个 baseline/gated 配对 forward pair；五个 condition 均为 `completed`。clean 的 `2,180/2,180` 个 view 和 `218/218` 个融合结果严格相等；四个受损 condition 的 Depth 变化均未越出 mask，全部 mask hash 与 v3 source manifest 一致，q=0 Depth8 等价、有限值、原始 Label 网格、配对 RNG 和 138 组统计门禁均通过。主条件 `boundary-q75` 的 Oracle-minus-baseline Boundary IoU 为 `-0.1508352015` 个百分点，95% percentile interval 为 `[-0.2676580460,-0.0462325573]`；mIoU 为 `-0.2316731726` 个百分点，95% interval 为 `[-0.3914881430,-0.0919002976]`，两个 effect 均为 `138/138` 配对组，因此按冻结 C 裁决为 `oracle-not-supported`。位置特异性 Boundary IoU 为 `-0.0926464932` 个百分点、mIoU 为 `-0.1344022510` 个百分点，等于 boundary-q50 gain 减 nonboundary-q50 gain，没有重复乘 100。运行 artifact 内计时 `15,827.803` 秒，execution 墙钟约 `15,860.917` 秒，峰值 CUDA memory `5,703,501,824` bytes；`official_test_included=false`。canonical `full-evaluation.json` SHA-256 为 `f5cadf93ace37868b27ecef2f5a96c18702ba821b8eda242f3c7921982a42f12`，mask manifest SHA-256 为 `23675b08f8d17fd5d528d7afe181330f39578dd08958a1447cc2761af5c68656`，execution 为 `cloud/DVG-B1-oracle-gsa-v1/executions/20260913T150505939412+0000-full.json`。大白话说，即使直接知道真实坏区，这个最小 GSA 深度门控也让主指标变差，所以按预注册规则停止该方案。
- 2026-09-13 16:32 UTC，已从关闭的 DVG-B1 转入独立 `MMFR` 多形式模态失效与可学习可靠性方向。方向级裁决是：MUSeg 适合矿井域受控合成失效的开发研究，但因缺少自然故障标签、故障率、重复采集和标定漂移真值，不能单独支持真实部署可靠性结论；失效不再等同于 Depth 置零，而分为整模态缺失、局部缺失、噪声、模糊、Depth 量化、Depth 几何错位及由易到难的混合条件。已新增 standalone `utils/dataloader/multimodal_failure.py` 和 `models/modal_reliability.py`：前者输出确定性 corruption、可审计 metadata 和 $R=\exp(-\sum b_k)$ 连续 target，后者提供 14 通道固定可微信号特征、两模态 reliability head、四级 area pyramid、连续 BCE 和初始化严格 scale=1、范围在 `(0,2)` 的 geometry adapter。主代理复跑静态编译、内联 CPU probe 与静态诊断通过；未修改现有 TrainPre/Dataset/DFormerv2/config/evaluator/checkpoint 路径。DFormerv2 仍是 RGB-centric，当前 B1 只能研究 Depth geometry reliability；覆盖 RGB 完全失效必须另建双语义路径。大白话说，工具和可学习模块已准备好，但尚未接入训练，更没有新模型效果；下一步先冻结 A2 的 RNG、target 几何同步、raw/normalized 输入、loss、clean control 和独立 protocol/config，训练、GPU、云资源与 official test 均未授权。
- 2026-09-14 00:56 UTC，`MMFR-A2-train-integration-v1` 已完成协议冻结、限定代码接入和主代理 CPU qualification。corruption 固定在 DataLoader 最终 mirror/scale/crop/pad 后、GPU 前由主进程执行；每样本使用训练 seed `2026091402`、epoch、iteration、global rank、sample slot 与 sample-id SHA-256 words 构造独立 NumPy `PCG64`。当前训练 config 只采样 Depth，`p_clean=0.25`、`max_specs=2`，总损失为单次输入 segmentation loss 加 `0.1` 倍连续 reliability BCE，consistency 为 0；reliability 预测不进入 backbone、decoder 或 geometry prior。已新增 `utils/dataloader/mmfr_training.py`、三个独立 A2 config 和 protocol template，并在 `models/builder.py`、`utils/train.py` 增加配置关闭时保持旧路径的可选接口。主代理直接复核并修正 Depth 通道一致性、Depth-only 抽样顺序、inverse-normalized pad 伪值可能经 misalignment 移入有效区，以及 reliability 字段静默缺失四类风险；静态编译、exhaustive uint8 round-trip、确定性、clean exact no-op、pad/target 中性、Depth curriculum、finite loss/backward、静态诊断和 `git diff --check` 通过。没有运行 GPU、训练、checkpoint load、真实 Ham forward、evaluator、完整测试、云任务或 official test。大白话说，训练接线和随机规则已经核对到 CPU 级别，但真实模型还没在 GPU 上走一步，不能当成训练成功或方法有效。
- 2026-09-14 01:35 UTC，用户单独授权的 `MMFR-A2-depth-corruption-train-v1` 本地 GPU 单步 preflight 已完成。首次真实 AMP forward 暴露 reliability 固定信号特征中的梯度平方乘积在 FP16 下溢，导致余弦分母为零并产生非有限值；现将小型 reliability auxiliary branch 固定为 FP32，segmentation backbone/decoder 继续使用 AMP。随后 probe 已完成真实 forward/backward/AdamW update，但短运行结束时原 checkpoint selector 仍要求不存在的 `latest.pth`；现已让 probe 不创建或 finalize selector，同时保留 `training_result.json`。最终 canonical 运行使用本地 RTX 5060 Laptop GPU、官方 pretrained SHA-256 `19116988fc86dc9f3e879282237941e11b9b1b5c480edb51e92807311dbc11a6`、真实 `train-dev`、batch size 1、worker 0、SyncBN、AMP 和 Ham decoder；由于初始 GradScaler 溢出，共尝试 6 个 batch、跳过前 5 次 update，最终完成 1 次 optimizer update，6 个 loss 均有限，包含 1 个 clean 与 5 个 corrupted sample。最终更新步 loss 为 `3.3470830917`，峰值 allocated/reserved CUDA memory 为 `2069.91/2260` MiB，结束时 free ratio `0.5812`，安全门禁通过。canonical preflight 物化 `protocol.json` SHA-256 为 `e9825c4a4818cf2860a529b6c0fa542f5412a7153a1e8b9b8c8d000de6ca7f07`，preflight 状态回填后的 repository template SHA-256 为 `b5b5c979352cca451cfbca35abc77232a9ae82c3f22ec0f33fcdb964ae03e519`；仓库内忽略目录 `outputs/mmfr-a2-gpu-preflight-20260914T0138Z/` 中 `run_config.json`、`training_result.json`、`probe-telemetry.jsonl` 的 SHA-256 分别为 `2ff6bf4668efbb58012fced89af698ce4cfec5181c43a4ee4dec9e56dce4edd3`、`bb3d71dacc99d7b1b97eb03d702f0eff621f78669b7dbb5824aaafad832550c0`、`6ae56d55552aee9cacda71b09fea7269069814e18d81e1d3548539d0995d462c`；run config 显式记录未提交工作区 `dirty=true`、`official_test_included=false`。大白话说，真实 GPU 训练链已能完成一次有效参数更新，preflight 中发现的两个阻塞也已修好；当前仍未开始 500 epoch 正式训练，没有 checkpoint 或模型效果结论。
- 2026-09-14 02:02 UTC，用户确认正式训练以云端单 GPU 为准，本机仅用于推理、想法初步验证和小规模 preflight。按价格快照，RTX 4090 为 `1.88 元/小时`，RTX 5090 为 `2.78 元/小时`；5090 只有在相同配置下吞吐达到 4090 的 `1.4787` 倍以上时，单位样本成本才可能更低。默认选择 RTX 4090 24GB；仅在 4090 缺货、batch size 10 无法通过 24GB 容量门禁，或配对短 probe 证明 5090 单位样本成本更低时使用 RTX 5090。正式配置保持云端单卡、batch size `10`、DataLoader workers `8`、AdamW、learning rate `6e-5`、500 epoch、warmup 10 epoch、AMP、SyncBN、首轮关闭 `torch.compile`、seed `772961337`；换卡不得改变科学超参数。当前没有创建云实例或启动训练，下一步仍需单独授权云端容量/吞吐 probe 与两个公平对照训练。大白话说，最省钱的默认方案是等 4090，有急迫启动需求且 4090 无库存时才租 5090；本次只是把规则写死，没有产生费用。
- 2026-09-14（v2 修订批次），MMFR 方向已在正式 500 epoch 尚未执行的时点完成协议修订，替代身份为 `MMFR-A2-train-integration-v2`。v1 模板状态改为 `superseded-before-formal-training` 并保留其科学语义、CPU qualification、本地 GPU 单步 preflight 事实与历史哈希；v1 模块与 config 未被改写。v2 的实质修订是四条：severity 只编码一次（`gaussian_noise`/`blur`/`quantization` 的连续 burden 就是 realized normalized damage，`DAMAGE_REFERENCE=0.25`）；`blur` 与 `misalignment` 的 severity 改为相对图像尺度（`BLUR_SIGMA_FRACTION=1/80`、`MISALIGN_MAX_SHIFT_FRACTION=1/30`）；Depth 监督目标改为 $R_D^{sup}(p)=V_D^{pre}(p)\cdot R_D^{syn}(p)$ 并新增 `depth_valid_pre`/`depth_valid_post`；reliability 辅助损失只监督 Depth 通道，RGB 通道保留为未计分的 all-ones scaffold。这些失效统一定性为 model-input/representation-level synthetic corruptions，不是 Kinect 真实物理噪声模型。新代码为 `utils/dataloader/multimodal_failure_v2.py`（SHA-256 `8734cf090e58eca17a54add761bbddf8baa06712481333d35a45e8bf4b8e94b1`）、`utils/dataloader/mmfr_training_v2.py`（`c5fb035eee040d8afd05007a4d84a2912afe70d2f2b3cc198ad5eafb441e7d0c`）、三个 `*_v2` config，并在 `models/builder.py`（`96ce2f4a61e62ae80ff732e387d3ec83486be5c63f8d528d28996e719901c278`）与 `utils/train.py`（`f7a24349dea295de42b322c97c263fcfde44e3e606ba41034b15589b5ca79099`）增加 v1/v2 双协议守卫与 v2 记账遥测。大白话说，协议在结果产生前被修正，旧证据保留，新身份另起，两套互不导入；这些哈希对应未提交工作区字节。
- 2026-09-14（v2 修订批次），`tools/mmfr/severity_burden_audit.py`（SHA-256 `7300a9e48c5f0dca59762f3fa47f99f81e4f6e2f7dae9a5308a2e44d8f96a796`）已在 CPU 上运行通过：退出码 `0`、`OVERALL: PASS`、`violations=0`，确认 `no_double_severity_encoding`、`monotonic_burden`、`monotonic_target`、`relative_scale_consistent`、`dropout_extent_monotone`、`empty_spec_strict_noop` 全部为真；报告为 `outputs/mmfr-a1-v2-severity-audit/severity-burden-audit.json`，SHA-256 为 `86e468c040648a790675456db1b828a5a71e560b849c75b4b7e9a3ca926c2f52`，主代理复跑得到同一哈希（7.9 秒）。审计使用两个确定性合成 fixture（`480×640` 与 `932×1082`），不是真实样本。大白话说，损坏强度与 burden 的映射关系已被机器逐条核对，且“强度越大越坏”的单调性成立。
- 2026-09-14（v2 修订批次），A2 v2 已通过主代理的 CPU 定点检查：以真实 `train-dev` 样本（`RGB/06-01-01-0035-230920140169-12-99.jpg`，冻结 `480×640` crop、合成 pad、注入原生无效 Depth 区块）调用 v2 batch helper，`47` 项断言全部通过、失败 `0`，覆盖 config 冻结字段守卫、输出契约、clean 精确 no-op、clean 目标等于原生 Depth 有效性、pad 中性且恒为精确零、corrupt 只改 Depth 且 RGB 逐位不变、Depth-only 监督、target/raw 值域与有限性、`depth_valid_pre`/`depth_valid_post` 为 `valid_mask` 子集、invalidity 记账闭合、同 seed 复现与 `p_clean` 双分支可达（40 次迭代 13 clean / 27 corrupt）。实测计数为 `valid_pixels=272384`、`natural_invalid_pixels=267802`、`synthetic_missing_pixels=871`、`newly_valid_pixels=132827`、`post_corruption_invalid_pixels=135846`。同一检查发现并修复一个真实缺陷：原 invalidity 记账断言 `post == natural + synthetic`，但 `gaussian_noise` 与 `misalignment` 会把原生无效像素变成非零，使该等式必然失败并中止首个真实 corrupt 样本；现改为分开记录 `newly_valid_pixels` 并断言闭合平衡 `post + newly_valid == natural + synthetic`，仍保持 fail-closed（v1 无此记账，未受影响）。该脚本是一次性临时内联脚本，未留在工作区；未运行任何真实模型 forward/backward，因此 segmentation 路径、AMP 与 reliability head 接线只经过静态复核。大白话说，训练接线的语义已经在 CPU 上逐项核对过，但真实模型这一步还没跑。
- 2026-09-14（v2 修订批次），单样本核对发现 MUSeg Depth 原生稀疏的事实：`train-dev` 样本 `06-01-01-0035-230920140169-12-99` 的 Depth8 与 Depth16 原始网格均为 `932×1082`，其中 `993,379 / 1,008,424` 个像素为 `0`（约 `98.5%`）。因此 A1 v2 的“原生无效 Depth”定义会把该样本大部分像素的监督目标设为 0；这是 MUSeg 作者保留“Depth 信息缺失但 RGB 清晰”样本的直接后果，也是 v2 必须区分原生无效与合成缺失的原因。该数值只是单样本核对结果，不得当作数据集整体统计。大白话说，矿井深度图本身大量缺失，这正是这套失效定义要处理的对象。已登记为待确认的开放项：是否允许 corruption 在原生无效像素上产生非零 Depth（`newly_valid_pixels`），当前实现只记录、不改变语义。
- 2026-09-14（v2 修订批次），结果前冻结的三个补充协议模板已建立：`protocols/mmfr-r1-reliability-supplemental-v1.template.json`（SHA-256 `5ea5ff1df795f14c3c92345c80ac6112814e7e3f49b85e479904632b0419960b`，三层 reliability 专门评价、`explicit-invalid` 与 `implicit-quality` 必须分开报告）、`protocols/mmfr-s1-severity-sweep-v1.template.json`（`bc3fb11666239885b7985778923dee55f37c7bbd1209c395b5bfc667cbe0eb9d`，severity `0.25/0.50/0.75` × 五类 + `entire_missing@1.0` + clean、mIoU-vs-severity 曲线与五类宏平均 robustness-AUC，不得用于 checkpoint selection）、`protocols/mmfr-c1-paper-confirmation-v1.template.json`（`f5ef2288fffff79185156ed1245aa0e00ba8b6dca73bcea0d2f79ee9ec29a3cd`，论文/SOTA 确认阶段：官方 `1595` train / `1576` test、500 epoch、3 个 paired seeds、sealed official test 单次读取）。三者都标注为未授权、不可执行，且起草期自拟的数值（R1 的 Spearman 阈值 `-0.10`、AURC/Brier `0.05`、Holm-Bonferroni 家族 `12` 与 α `0.05`、bootstrap `10000`、coverage 点 `20`；S1 的容忍度 `0.25` 个百分点与允许违反 `0`；C1 的 bootstrap `10000`）已显式标为 `provisional-default-not-frozen-requires-user-confirmation`，执行前需用户确认，不得当作已冻结阈值。大白话说，指标和分组已经锁死，但其中一批具体数字是我们自己拟的，必须由用户点头才算数。
- 2026-09-14（v2 修订批次），v2 结构化身份与门禁已写入 `protocols/mmfr-a2-train-integration-v2.template.json`，状态为 `code-revised-cpu-checked-gradient-path-isolated-training-unauthorized`（补入门禁证据与复跑记录后的当前哈希：CRLF 原始字节 `8ce4e7c40e04d080ab93cf8b299500f3ca621a0b9ce9757c788fe1435f1a990a`，LF 归一化 `39b43fb7bd905b4ca49a9e9707227c833dea23c7ea7fb3259f3711ae2571b58d`）；v1 模板仅追加状态块后为 `6cb29725e4972526089eb85e8f9e07cf383c19b2389601e189cb69e2d28cb23a`，其科学语义与历史事实未改。`tools/mmfr/gradient_path_isolation.py`（SHA-256 `522a475f8a524bb4fe27379be7882535208de6db19940fb8ec62f0431ef44435`）当时**已写好但从未执行**（用户当时明确要求暂不运行）：它要求在相同模型状态、输入、corruption、RNG 与 segmentation loss 下比较 `lambda_rel=0` 与 `lambda_rel=0.1` 两次 backward 的共享参数梯度必须一致、reliability head 梯度只应出现在后者、head 开/关时 logits 一致。**该状态已于 2026-09-15 变更：该检查已获用户授权并实测通过，见本节 2026-09-15 条目；正式训练本身仍未授权。** 大白话说，这道“证明可靠性头不会影响分割梯度”的检查当时还没跑，现在跑过了。
- 2026-09-14（v2 修订批次），审计总规划已改写为 v2 版本：`liu-test-exp/方案1/MMFR-多形式模态失效可靠性学习-独立审计总规划.md`，并在其中逐条登记 13 条改动指令的处置、三个补充协议身份、B1/B2 边界与全部新增风险项；同目录 `doc/plans/2026-09-MUSeg-多形式模态失效可靠性学习/` 的 `00-总方向规划.md`、`01-新对话最小上下文与当前任务.md`、`02-MMFR-A1失效基函数与可靠性脚手架.md`、`03-MMFR-A2训练接入与公平对照协议.md` 已同步 v2 语义，历史 v1 记录与哈希原样保留。供外部高级模型审计的配套材料为同目录 `改动细节.md`，其中记录逐文件改动细节、13 条指令逐条处置、三项实测资格检查的命令与数值、未执行事项、已知不一致（含 v1 历史哈希不可复现与行尾双写法）以及哈希总表。大白话说，文档已经不再按旧协议描述当前设计，并且有一份专门的“这次到底改了什么”的材料可以交给外部审计。
- 2026-09-14（v2 修订批次），**发现一处待核验的证据完整性问题：** v1 模板被记录的 `b5b5c979352cca451cfbca35abc77232a9ae82c3f22ec0f33fcdb964ae03e519` 与 v1 manifest 被记录的 `e9825c4a4818cf2860a529b6c0fa542f5412a7153a1e8b9b8c8d000de6ca7f07` 无法从当前工作区字节复现（原始字节、LF 归一化与六种 JSON 规范序列化均已测试，均不匹配）；`outputs/mmfr-a2-gpu-preflight-20260914T0138Z/protocol.json` 的当前原始字节 SHA-256 为 `03443995dbaf03b0750fe736193ef2a1bc3b6f0711987ab634fdff6af5bbef4a`。历史值未被改写，该差异标为**待核验**，不得据此断言文件被篡改，也不得忽略。**行尾约定（同一内容的两种编码，不要误判为改写）：** 本仓库 Windows 检出下大量文本为 CRLF，而部分记录值按 LF 归一化内容计算；v1 模板当前以 LF 存储、原始字节哈希为 `a3002b731019517cbd62b83cfd14e71260099efdd8290504a3ad8bbf6d46ef51`，其 CRLF 编码结果为 `6cb29725e4972526089eb85e8f9e07cf383c19b2389601e189cb69e2d28cb23a`（即本次仅追加状态块后按 CRLF 记录的旧值），两者内容完全相同。大白话说，旧协议里记的两个哈希现在按文件重新算不出来，这需要单独查清；同时要注意有些“哈希不一致”只是行尾不同造成的假象。
- 2026-09-15 01:32（本地时间），用户单独授权的 `MMFR-A2-v2-gradient-path-isolation` 门禁已在本地 NVIDIA GeForce RTX 5060 Laptop GPU 上实际执行并通过：`python tools/mmfr/gradient_path_isolation.py` 退出码 `0`，墙钟 `15.3` 秒，结论 `gradient-path-isolation: PASS`，`failures` 与 `warnings` 均为空。实测结果：**714 个共享参数**（backbone `672`、decoder `13`、geometry prior `29`，规则为所有不以 `reliability_estimator.` 开头的参数）在 `lambda_rel=0` 与 `lambda_rel=0.1` 两次 backward 下**逐位完全相同**（`max_abs_difference=0.0`、`max_relative_difference=0.0`、`over_tolerance_count=0`、`absent_in_both_runs_count=0`，容差 `ATOL=1e-6`/`RTOL=1e-6`）；reliability head 的 `6` 个参数在 `lambda_rel=0` 时梯度存在且恒为零（范数 `0.0`），在 `lambda_rel=0.1` 时存在且非零（范数 `0.05855773380379924`，最大绝对值 `0.04926614463329315`）；两次的 segmentation loss 逐位相同（`2.545051336288452`），reliability head 开/关两种 forward 的 logits 与 `lambda_rel=0` 的 logits 逐位相同。输入为真实 `train-dev` 的 `sample_index=0`、`iteration=0`，抽到 corrupted 样本（`clean=False`），`1` 个 spec、kind 为 `quantization`（`levels=195`、`step_uint8=1.3144329896907216`，severity 约 `0.24`），分割有效像素 `11932`。证据为 `outputs/mmfr-a2-gradient-path-isolation/gradient-path-isolation.json`，SHA-256 `dd0e351b418f0228650830c70cf0750c21a446502d3a9c11ebc047581aedb763`，stdout 记录为同目录 `run-stdout.txt`；该 JSON 不写 checkpoint、不读取 official test。工具文件哈希的两种写法指同一份内容：规划文档中的 `522a475f8a524bb4fe27379be7882535208de6db19940fb8ec62f0431ef44435` 是 LF 归一化值，而在本 Windows 检出上以 CRLF 存储时原始字节哈希为 `002dbf0559a1d2989fbe3c3c45f7fb2c39a8e319bd041192d1bc7fa0b1233474`，后者正是本次运行在报告里记录的 `tool.sha256`，因此“实际执行的工具”与“被冻结的工具”除行尾外逐字节相同。**复跑确认：** 主代理随后以 `--output outputs/mmfr-a2-gradient-path-isolation/repro-check.json` 复跑一次，得到完全相同的判定与数值（同一 corruption 抽样 kind、同一 segmentation loss `2.545051336288452`、同一 head 梯度范数 `0.0`/`0.05855773380379924`、两次 sweep 的 logits SHA-256 均为 `82ec467dd97a8e44d7c3e11842ff15d9410d0f7033762d6670c3a43f5ed95288`），复跑报告 SHA-256 为 `54a6c183877c32f720854461c0d62ffe0b3be512c729fff2c6a1b95367cbc6a5`，说明该判定是确定性的，不是单次抽样的偶然结果。大白话说，这不是“碰巧过了一次”，而是同样条件下重复运行给出同一答案。同一运行同时核到官方 pretrained 的加载事实：checkpoint key `785` 个、backbone key `780` 个、重叠 `774` 个、重叠张量不匹配 `0`，checkpoint 缺少 `extra_norms.0/1/2.{weight,bias}` 共 `6` 个 key（即模型有而 checkpoint 没有，因此这 6 个张量由仓库自身初始化提供，只作事实记录，不作延伸推论）。**证据边界：** 该检查是 batch size `1`、FP32（关闭 autocast 与 TF32）、单进程（无 process group 时 SyncBN 回退为逐进程 BN）、一个真实样本、一次 corrupted 抽样的定点检查，不覆盖冻结 batch size `10`、AMP、DDP、多样本、完整 epoch、checkpoint save/load、evaluator、云资源或 official test，也不提供任何 mIoU 或鲁棒性收益结论。**通过后冻结的结论：** A2 分割性能变化的直接原因是 corruption exposure，reliability head 在该阶段只承担 estimator qualification，因此不需要再额外消耗一次完整 500 epoch 的 corruption-only 训练来证明这一点。大白话说，这道门禁已经从“只是代码上看起来隔离”变成“实测确认隔离”：打开可靠性头完全不改变分割网络的梯度，所以未来 A2 的任何分割变化都不能记在可靠性头账上。
- 2026-09-15（v2 本地资格门禁批次），第二轮审计要求的本地资格证据已全部补齐，逐项如下。
- **`MMFR-A2-v2-initial-state-equivalence`（已通过）**：工具 `tools/mmfr/a2_v2_initial_state_equivalence.py`（SHA-256 `7637b67d211e43a872bfa7e9141e9fc6ff04c3647e51b35409401797ca7c1088`），六个独立子进程构建（每个身份 3 次），退出码 `0`、墙钟 `42.7` 秒、`40` 项断言 `failed=0`。结果：共有参数 `714` 个、共有 buffer `88` 个**全部逐位相同**，`max_abs_difference=0.0`；参数集合差异只有 corruption 侧的 `reliability_estimator.head.net.{0,2,4}.{weight,bias}` 共 `4,386` 个元素，另有 4 个 reliability 专属 buffer。`extra_norms.{0,1,2}.{weight,bias}` 六张量在六次构建中哈希唯一、可复现，weight 恒为 `1.0`、bias 恒为 `0.0`，官方 pretrained 提供 `0/6`（由仓库自身初始化提供）。**这一项把 `extra_norms` 确定性从“待核验”转为“已核验”**。证据 `outputs/mmfr-a2-v2-initial-state-equivalence/a2-v2-initial-state-equivalence.json`（SHA-256 `17dcb4e42f914b095a8d4187adad4c404e89e9eaeb857bd218e3ae47923c61fa`）。边界：纯 CPU、无 forward/backward、无数据读取，只覆盖构造完成时的初始状态。
- **同一次核验的附带事实（重要）**：本代码库的 `utils/init_func.py:group_weight` 只把 `720` 个参数张量中的 `677` 个放进 AdamW 的 param group，**`43` 个张量不在任何 group 里，因此从不被更新、也从不进入 GradScaler 的 inf 检查**：`29` 个 `backbone.*.Geo.weight`（几何先验标量，裸 `nn.Parameter`）、`8` 个 `backbone.patch_embed.proj.{1,4,7,10}.{weight,bias}`、`6` 个 `backbone.layers.{0,1,2}.downsample.norm.{weight,bias}`。两个身份同样受影响，因此不破坏公平性，但它意味着这些层在**任何**以此代码库训练的 DFormerv2 运行（包括历史 Quick-B0 基线）中都停留在初始化值。已登记为开放项，是否处置需要用户决定。
- **`MMFR-A2-v2-amp-update-path-isolation`（已通过）**：工具 `tools/mmfr/a2_v2_amp_update_path_isolation.py`（新工具，可复跑；SHA-256 见协议模板与 `改动细节.md`），在本地 RTX 5060 Laptop 上以**冻结 batch size `10`**、真实 `train-dev` 批次、真实 v2 Depth-corruption 模型与正式 AMP + GradScaler 路径运行，退出码 `0`、墙钟 `88` 秒。结果：`16` 次尝试获得 `10` 次真实成功更新（`13` 个 corrupt step）；两条轨迹的 **step/skip 序列完全相同、scale 轨迹完全相同**（`65536→32768→16384→8192→8192→…→1024`，共 6 次 skip）；每次成功更新后**共享参数逐位相同**（`0` 处不一致）、**共享 optimizer state 逐位相同**（`0` 处不一致）、`max_abs_difference=0.0`，最终共享参数仍逐位相同；reliability head 自身哈希发生变化（只在 `lambda_rel=0.1` 侧学习），两次 loss 不同（如末步 `2.5401` vs `2.6073`），证明辅助项确实激活而共享轨迹未被带动。峰值显存约 `2.50 GB`（batch size 10 在 8 GB 卡上可容纳）。证据 `outputs/mmfr-a2-v2-amp-update-path-isolation/a2-v2-amp-update-path-isolation.json`（SHA-256 `7020c354a948cb876a369dd196d03e21fd17fb1b3e051734b1563931ae358cdb`）。边界：单卡单进程、`16` 步、一次配对运行，不覆盖 DDP、完整 epoch、checkpoint save/load、evaluator 或 official test。
- **`MMFR-A2-v2-cpu-qualification`（可复跑版本，已通过）**：`tools/mmfr/a2_v2_cpu_qualification.py`（SHA-256 `b3737f1b7ea6947be9cd9db6ef6b910a4f661e9ddb29da72cc3ddf97475cc999`）把原先“运行后删除的一次性 47 项检查”固化为仓库内脚本并扩展到 `74` 项断言，主代理复跑得到 `assertions=74 failed=0 status=PASS exit_code=0`（`2.8` 秒）。原 47/0 结果保留为历史，不覆盖。证据 `outputs/mmfr-a2-v2-cpu-qualification/a2-v2-cpu-qualification.json`（SHA-256 `75436cf432a4877d3af63d6b16d8f6413f7007d08c8072cada08a1b2dccd7b57`）。
- **`train-dev` Depth 有效性分布审计（事实，已形成）**：工具 `tools/mmfr/train_dev_depth_validity_audit.py`（SHA-256 `62a52cf3f4c74352359506d753c69e502cbf3d35314220e1df361ba5b50e4d30`），扫描全部 `1277` 条 `train-dev`，退出码 `0`、耗时 `215` 秒。**原始对齐网格** `depth_valid_pre` 比例：mean `0.6833`、median `0.7413`、std `0.2482`、min `0.0125`、max `0.9999`；`<1%` 为 `0` 条、`<5%` 为 `13` 条、`<10%` 为 `26` 条、`<25%` 为 `91` 条、`<50%` 为 `309` 条；**没有任何一条样本 Depth 全为 0**。**训练几何（TrainPre crop/pad）**：mean `0.6978`、median `0.8177`，`<1%` 为 `27` 条、`<5%` 为 `56` 条、`<25%` 为 `161` 条、`<50%` 为 `322` 条，另有 `8` 条样本在 `valid_mask` 内完全没有有效 Depth。location group 按仓库既有冻结规则（`tools/mve/dvc_a1_core.py` 取文件名前四段）共 `762` 组。**监督像素构成（early/mid/late）**：target 恰为 `0` 的监督像素占 `28.33%` / `28.41%` / `43.84%`，其中约 `25.82` 个百分点在三档都来自原生无效 Depth。结论：BCE **没有被 target 0 完全支配，但确实有约四分之一到五分之二**的监督像素提供恒定 0 信号。证据 `outputs/mmfr-a2-v2-depth-validity-audit/train-dev-depth-validity-audit.json`（SHA-256 `63ea91947b699a99d57358239ccdbb93f1edf1d1aa6a81a67c559f0ad0f5af78`）。**同时更正历史印象：** 此前记录的单样本“约 `98.5%` 像素为 0”只是那一个样本的事实，不能外推；`train-dev` 整体原图平均有效率为 `68.3%`，最稀疏样本也有 `1.25%` 有效。
- **`MMFR-A2-v2-validity-transport-audit`（事实，已形成，含开放决策）**：工具 `tools/mmfr/validity_transport_audit.py`（SHA-256 `f574d461eb7da9a895251ca1cbc127ae72e7f5c611c401b193cda7e40407f7c4`），CPU、三次运行同一哈希，退出码 `0`。结果：`gaussian_noise`、`blur`、`misalignment` 会把原生无效（raw Depth == 0）像素变成非零；`quantization`、`spatial_dropout`、`entire_missing` 从不产生。在最稀疏的真实样本上，`gaussian_noise@0.25` 单个 `480×640` crop 就制造 `129,563` 个 `newly_valid` 像素（severity 1.0 时 `132,929`），约占该样本有效区内原生无效像素的 `48%`；`R_D^sup = V_D^pre · R_D^syn` 逐位成立、记账恒等式在全部 `84` 条记录上闭合——**这恰好说明记账闭合不等于 target 语义正确**。misalignment 的四类运输关系里“源无效→目标非零”结构性恒为 `0`，平移前后有效判定一致率为 `77.57%–99.39%`。工具输出 `decision_required` 区块（选项 A：原生无效为吸收哨兵、intensity corruption 只能作用于已有有效 Depth；选项 B：允许 spurious depth 但必须显式命名为 spurious-measurement corruption；另有 misalignment 子问题：validity 是否应随 Depth 一起做空间搬运），**未做任何选择**。证据 `outputs/mmfr-a2-v2-validity-transport/validity-transport-audit.json`（SHA-256 `4cc92b5f6885e9e8163062062e3d53a843752699e4e8bebd77e5d9dad81639fa`）。
- **R1/S1/C1 已按第二轮指令第 7 条清理**：删除起草期自拟且无文献或前置假设支持的阈值——R1 的 Spearman `-0.10`、AURC/Brier `5%` 相对改进、Holm-Bonferroni 家族 `12` 与 α `0.05`；S1 的 `0.25` 个百分点容忍度与允许违反次数 `0`。三个模板现在只预注册指标、分组、估计量与置信区间，并显式声明**不设置任何新的成功/失败 gate**、不得替代主 A2 门槛；`bootstrap=10000` 与 95% 区间只作为计算精度设置保留。
- **v2 source identity 历史冻结：** 本轮以本地 commit `3c8ebddeb76e63a5be37e261381117abfba117ca` 与 annotated tag `MMFR-A2-v2-pretrain-freeze` 固定 v2 科学代码、config、protocol 模板、工具与文档记录（未推送远端；`outputs/` 被 gitignore 排除，证据以哈希指针记录）。该时点仍等待高级审计，现已由下一条 v3 裁决取代；v2 文件和资格证据不改写。
- **2026-09-15 v3 有效性语义与五项本地资格（已通过）：** 高级审计正式裁决为 A + MID-A，并明确 natural-invalid 不重加权、不删除 8 个全无有效 Depth 的训练 crop、43 个 optimizer-missing 参数维持上游语义。v3 使用显式 sequential validity state，intensity corruption 只在 state 内作用，blur 为 mask-normalized blur，misalignment 同时搬运 Depth/state，最终 target 为 `V_state_final * R_syn`。五项 canonical 证据全部 `PASS`：severity/burden 报告 `outputs/mmfr-a1-v3-severity-audit/severity-burden-audit.json`（raw SHA-256 `34d9d70d1e1b9c7420fc7b293f2c2b3eb880caeb7fb8d4f8f6935078bdc8bbe0`）；CPU qualification `121/121`（`outputs/mmfr-a2-v3-cpu-qualification/a2-v3-cpu-qualification.json`，raw SHA-256 `b8740689adccb84249b35c7a98fe936dd229694efa6af071bf31582f085f934a`）；validity transport `48/48`（`outputs/mmfr-a2-v3-validity-transport/validity-transport-audit.json`，raw SHA-256 `9f3066d47e82a2ad84e2dad457a6b68c0d5dac4d28882249fdaca7495eb4cd90`）；initial-state equivalence `38/38`、`714/714` 共有参数与 `88/88` 共有 buffer 逐位相同（raw SHA-256 `4035612e5e314a9044626503c80c2af869a6be1a5cf4d89aab8383b7cb0515cc`）；AMP update-path isolation 在 batch size 10 下 `17` 次尝试、`10` 次成功更新、`17` 个 corrupt step，共享参数与 optimizer state 每次成功更新后逐位相同，峰值 CUDA allocated `3,132,888,576` bytes、耗时 `2112.328` 秒（raw SHA-256 `770daaeab50da4e593b0a722ef0b7482b4c67d8616a37d95348b6a4432036872`）。完整 raw/LF 双哈希与工具身份见 `protocols/mmfr-a2-train-integration-v3.template.json`。所有报告均为 `official_test_included=false`。大白话说，v2 暴露的“假深度复活”和错位坐标语义已在新身份下修正，并已用 CPU 与本地 GPU 门禁核实；这些仍不是训练效果。
- 2026-09-15 18:39（UTC+8），用户单独授权创建云端实例，并明确要求先创建、立即停机、停下等待人工确认配置。已创建 `cpod-1vbh7faqcauq`（名称 `mmfr-a2-4090-probe`）：`cn-bj2` / `cn-bj2-03`，RTX 4090 一张、14 vCPU、32 GiB 内存、50 GiB `CLOUD_SSD` 启动盘、平台镜像 `pytorch_2.1.2_Py3.10`（`compshareImage-17eq1w2rtxi3`，底层 `ubuntu2204_cuda118_torch212_py310`）、按量后付费。创建于 `18:38:55`，启动后约 `13` 秒即停机，最终状态经 `instance list --all` 与 `instance show --status` 双重核验为 `Stopped`。镜像选择依据是本仓库自身要求（`README.md` 指定 CUDA 11.8 / `pytorch==2.1.2` / `mmcv==2.1.0`）与历史云端已核验环境（Python 3.10.16、PyTorch 2.1.2+cu118、`/usr/local/miniconda3/envs/py310`，见 `doc/reports/2026-08-25-museg-stage04-cloud-qualification-handoff.md`），两者一致；**该实例内部的实际环境路径、驱动与 CUDA 版本尚未登录核验**。`InstancePrice` 记录为 `1.88 元/小时`；创建前 `instance create --dry-run` 报价为 `Instance 1.99`（原价与标价均为 `2.09`，折扣类型 `3`），磁盘报价 `0`，`1.88` 与用户控制台人工看到的值一致。同区另有此前已存在的同规格停机实例 `cpod-1vbgq9k8beqa`（名称 `host`，RTX 4090 一张、14 vCPU、32 GiB，镜像 `cuda132_torch2130_py312`，创建于 `18:25:34`，`InstancePrice` 同为 `1.88 元/小时`）；本批次未修改、未删除它，其去留**待用户确认**。停机计费口径沿用项目既有记录：按量实例在平台 `Stopped` 状态下 CPU/GPU/内存被回收并停止计费，云盘与镜像继续计费（来源 `doc/reports/2026-08-26-museg-stage05-seed1-prelaunch.md` 第 5 节）；平台文档页面另有“按量实例连续关机 7 天后自动回收资源”的规则，**该 7 天回收规则只来自平台文档，本批次未在控制台或账单独立核验**。**本批次只做创建与停机：未登录实例、未建立 SSH、未传输数据、未运行 probe、未开始训练。** 大白话说，机器已按你的要求建好并关机，配置等你确认；现在不产生计算费用，也没有做任何实验，另外发现一台同规格的旧停机实例需要你决定怎么处理。
- 2026-09-15 12:10（UTC，本地 20:10），**云实例 `cpod-1vbh7faqcauq` 已由用户重新启动并处于无卡模式，数据集已落地并完成核验**。本对话的执行主机名为 `cpod-1vbh7faqcauq`；用户表述当前为无卡模式，本批次**未做平台侧状态独立核验**（`nvidia-smi` 无输出，与无卡模式一致，只作旁证）。用户已在该实例完成 OpenList 夸克网盘配置，并把归档 `MUSeg_DFormer.7z`（`1,539,666,959` 字节）取回到 `/root/rivermind-data/dataset/`。**已核验事实：** ① `7z t MUSeg_DFormer.7z` 退出码 `0`、`Everything is Ok`、`12687` 文件 / `1573828935` 字节；② 解压产物 `/root/rivermind-data/dataset/MUSeg_DFormer/` 含 `RGB`/`Depth`/`Depth16`/`Label` 各 `3171` 个文件、`train.txt` `1595` 行、`test.txt` `1576` 行、`dataset_meta.json`（`max_raw=13932`、`split_source_sha256=4ab3526d3db37bf5c96c711560fc2a7a3b92758554d205ff327bce3f6740e5bb`）；③ 四模态主文件名集合完全一致（各 `3171`），`train.txt` 与 `test.txt` 互斥且并集等于全部样本；④ `train-dev`（`1277`）+ `val-dev`（`318`）= official train `1595`，且 `train-dev`/`val-dev`/`official-test` 的全部 `3171` 条成员在数据目录中存在，`official-test` 与开发划分交集为 `0`；⑤ 按仓库 `tools/splits/create_museg_dev_split.py` 的 inventory 序列化规则重算 official-train 的 `RGB`/`Label`/`Depth16` 哈希与字节数，与冻结 `data/splits/MUSeg/dev-v1/manifest.json` 记录**逐位一致**（`c610b0cdf76ac70dc718305e604601a9cb79280d91117586f5d42e9ca73a3c62` / `f3a22430dd81f29da741af93e0d8b4d7f4e1981e7bd2291c24c55d4997db5ffd` / `b5107c27bfdad0b296c72a77c6c7fde91dc02608f7dffc85005b950388e528b5`）；⑥ 随机 `20` 个样本核对 `round(Depth16*255/13932) == Depth8`，最大绝对差 `0`，`Label` 为 `uint8`、`(932,1082)`、最大值 `14`。**重要身份差异：** 该归档是 `2026-08-19` 生成、**已经转换好的 `MUSeg_DFormer` 包**（内部根目录为 `RGB/`、`Depth/`、`Depth16/`、`Label/`、`train.txt`、`test.txt`、`dataset_meta.json`），不是 `doc/guides/cloud/openlist-quark-download.md` 记录的原始 `MUSeg.7z`（`2049552128` 字节）；因此**不需要也不能**再对本归档运行 `tools/prepare_museg.py`，且本实例上不存在原始 `MUSeg/` 与 `DatasetSplit.zip`。磁盘 `/root/rivermind-data` 剩余约 `46G`，归档 `1.5G` 随后在用户明确确认后于 `12:13`（UTC）删除（`rm /root/rivermind-data/dataset/MUSeg_DFormer.7z`，退出码 `0`；删除后剩余约 `47G`），解压数据目录 `MUSeg_DFormer` 保留。**高级审计对 probe 之后顺序的建议（记录为建议，尚未由用户冻结，也不构成任何执行授权）：** ① probe 中让 `MMFR-A2-clean-control-v3` 与 `MMFR-A2-depth-corruption-train-v3` 两个身份都至少真实跑若干 step，以确认 4090 环境无环境特异问题；② 正式 500 epoch 前补一次工程门禁 `checkpoint save → 进程销毁 → strict reload → optimizer/GradScaler/scheduler/RNG 与训练身份恢复 → 继续 step`（该文档记录的“checkpoint save/load 尚未验收”仍是长训练前最后一个工程风险）；③ 同一次云任务验证控制面停机流程并确认实例状态为 `Stopped`；④ 以上通过后才另行申请两个公平身份的 500 epoch 授权。另有一条不阻塞 probe 的解释性提醒：MID-A misalignment 下原本 `V_pre=0` 的目标坐标可能因合法 Depth/validity 平移进来而变成 `V_post=1`，因此训练 telemetry 的 `natural-invalid` 只表示“该目标坐标在 corruption 前原始 Depth 无效”，不等于“最终输入在该坐标无 Depth”；未来 R1 的 `explicit-invalid` 应按最终 `V_post=0` 定义。本批次未运行训练、probe、evaluator、GPU 任务，未创建或销毁任何云资源，official test 仍为 `sealed_unread`。**仓库事实：** 本实例检出为分支 `research/multimodal-failure-reliability`，HEAD `a917ad32664955691a69d0be3eeda88e9d248388`（提交信息 `上云`），与 `origin` 同名分支完全一致（ahead/behind 为 `0/0`），即 v3 冻结提交已随该分支推送到 `origin`；本检出中**不存在任何本地 tag**，因此 `MMFR-A2-v2-pretrain-freeze` / `MMFR-A2-v3-pretrain-freeze` 是否已推送远端**待核验**。大白话说，机器已启动并处于无卡模式，数据集已经从夸克网盘取回到服务器并逐字节核对过，可以直接用于后续训练；下载用的 1.5G 压缩包已按你的确认删除，被核验过的数据目录保留。
- 2026-09-15 13:05（UTC），**执行计划修订（用户第 2 轮要求 `liu-test-exp/方案1/改动说明.md`），不新建科学协议 v4**：在云端容量 probe 之后、正式 paired 训练之前新增阶段 `MMFR-A2-v3-exploratory-one-arm-screening`，只回答“`MMFR-A2-depth-corruption-train-v3` 是否表现出值得继续投入第二个 500 epoch clean control 的研究苗头”，明确它是成本筛选、不是正式因果实验、不改变主 success gate。新的执行顺序为：probe → 只申请一个正式 500 epoch（corruption-v3，从冻结官方 pretrained 独立开始）→ 冻结 checkpoint 后用现有主 evaluator、evaluation seed 和 failure conditions 评价，并把 Quick-B0 作为 `historical RGB development reference`（不得写成 `clean control`）→ 产出 one-arm-screening-report 后状态置为 `awaiting-senior-review-of-one-arm-screening` 并停下，不自动启动第二个 500 epoch → 高级审查后再决定是否单独申请 `MMFR-A2-clean-control-v3` 的 500 epoch → 通过后 Quick-B0 降级为普通历史参考，正式结论只比较 `Depth-corruption-v3 − Clean-control-v3`。第一阶段允许的结论只有 `screening-promising` / `screening-unpromising` / `screening-inconclusive`，禁止 `A2 supported`、`MMFR improves robustness`、`causal gain`、`正式成功`。v3 科学变量全部不变（六类 failure、severity、curriculum、`p_clean=0.25`、`lambda_rel=0.1`、500 epoch、batch size 10、optimizer、selector、evaluator、主 success gate、43 个 optimizer-missing 参数）；本轮只改执行顺序、授权状态和结果解释。**当前进度：** 本文件已记录该修订；`protocols/`、`doc/main/MUSeg-open-decisions.md` 与总规划的同步写入仍在同一批次内进行，未完成部分以本节为准。授权边界：probe 已由用户开启有卡模式并指示推进；两个 500 epoch 训练仍未授权，probe 授权不升级为训练授权。大白话说，省钱办法是"先只跑贵的那一份、看有没有苗头，值得再跑第二份"，正式结论仍然必须两个身份配对比较，Quick-B0 只能帮我们判断值不值得继续花钱。
- 2026-09-15 13:05（UTC），**云端 batch size 10 容量/吞吐 probe 已实际执行完成（两个身份各一次）**。实例 `cpod-1vbh7faqcauq` 处于有卡模式，GPU 为 RTX 4090（`total_memory=25,280,839,680` bytes），驱动 `610.57.04`，CUDA `11.8`，cuDNN `8.7.0`。**预训练权重：** HuggingFace 直连下载失败（HTTP/2 中途 `CANCEL`，仅得约 `49 MB` 残片，残片已删除），改用用户自传副本，大小 `110,203,103` 字节、SHA-256 `19116988fc86dc9f3e879282237941e11b9b1b5c480edb51e92807311dbc11a6`，与冻结身份逐位一致，已放置到 `/root/rivermind-data/pretrained/DFormerv2_Small_pretrained.pth`。**环境补齐：** `opencv-python 4.10.0.84`（补装 `libgl1`）、`timm 1.0.28`、`mmengine 0.10.7`、`mmcv 2.1.0`、`tensorboardX 2.6.5`、`easydict 1.13`、`swanlab 0.9.7` 等，`numpy` 保持 `1.26.3`；首次 probe 因缺 `mmcv` 失败（`models/decoders/ham_head.py` 需要 `mmcv.cnn.ConvModule`），失败目录归档为 `attempts/clean-control-v3-attempt1-missing-mmcv` 与 `attempts/depth-corruption-v3-attempt1-missing-mmcv`。两份 qualification probe 清单通过 GPU preflight（`pass=true`；`phase=qualification`、`run_kind=probe`、batch size `10`、workers `8`、AMP、SyncBN、RGB 契约、split 与权重身份全部 `OK`）；清单 SHA-256：clean `19176ed03ca8895bd227d613b934ccdf64f54c06e783f4d92cb4213b02f3a7a6`、corruption `07a71dae7ef9dfefcc1f73cfde99de0f3b6ea5b8c58fc032eb09dd208a008851`；preflight 报告 SHA-256：clean `4997904524d988eecc5c47f029e090eda0480599bd9c6005035c6ace8ab6e934`、corruption `8e15c63c9ac04594fafb864210d492a4efa25327306c71b4dbf0390a9392d920`。**结果（每个身份 60 个有效更新步、68 次尝试、8 次 GradScaler 跳过、loss 全部有限、`official_test_included=false`）：** `clean-control-v3` 稳定吞吐中位数 `16.43` 张/秒（约 `0.609` 秒/步），峰值 allocated `18,952 MiB`、reserved `20,556 MiB`，最小剩余显存 `3,068.6 MiB`（`12.73%`）；`depth-corruption-v3` 稳定吞吐中位数 `4.94` 张/秒（约 `2.02` 秒/步），峰值 allocated `19,046 MiB`、reserved `21,286 MiB`，最小剩余显存 `2,330.6 MiB`（`9.67%`）。corruption 侧第一次运行被本项目自己的显存安全闸按历史 `0.10` 剩余比例门槛中止（**不是 OOM**，当时自由显存约 `2.3 GiB`），已归档为 `attempts/depth-corruption-v3-attempt2-vram-gate-abort`；随后在**显式放宽到 `0.05`** 的门槛下重跑，取得完整 60 步证据，两次数值与门槛差异都保留在证据中。probe 产物 SHA-256：clean `probe-result.json 8d8ac335acc0f4a803f0c1e7ca9935adefb781482843ba3354aec1cc6a3d0626`、`training_result.json d1fc830a1e627d6bc869202603fd60c5429bdcb2a1e3b38c927598bbede14dfe`、`probe-telemetry.jsonl 59f3984ca764e2d57355824dac29867701d55d0523d445ba1cd49f5df0fddde6`；corruption `probe-result.json a8f3a614606572ccdb0f801f2ad9f196e81c55a748b231de3114841bbe3e24f0`、`training_result.json a017ed10984322321e0edef70ecea0498f63d1ff2cab42c3af9c819ae9da9c54`、`probe-telemetry.jsonl b64d98baa429d5ef12de505bb2510d3f0d89ad12f963f4d60ed4bd9164af599e`；证据根目录为仓库外 `/root/rivermind-data/cloud/mmfr-a2-v3-probe/`。**基于实测的保守预算**（`iters/epoch=128`、`500 epoch=64,000` 步，包含 probe 自身约 `35 ms/步` 的 telemetry 哈希开销，未计每 10 epoch 的 validation）：corruption-v3 纯训练约 `36.0` 小时、按 `1.88 元/小时` 约 `67` 元；clean-control-v3 纯训练约 `10.8` 小时、约 `20` 元；即“省钱策略”的第一段（corruption）显著贵于第二段（clean）。**尚未执行：** 审计要求的 `checkpoint save → 进程销毁 → strict reload → optimizer/GradScaler/scheduler/RNG 恢复 → 继续 step` 工程门禁、控制面停机流程验证，以及 SwanLab `online` 模式（本实例不存在 `/root/.config/dformer/swanlab.env`，probe 使用 `offline` 模式）。**一处待用户决定：** batch size `10` 在 4090 上两个身份都能跑完（corruption 峰值后仍剩约 `2.33 GiB`），但低于历史 `10%` 余量；500 epoch 训练期的 epoch 边界与 validation 显存尚未验证，处置方式（保持 4090 并接受该余量 / 先验证 epoch 边界与 validation / 改用 5090）需要用户确认。大白话说，容量测试通过了、速度和钱都量出来了，但"深度损坏"这一路每秒只处理约 5 张、比干净那一路慢三倍多，所以按省钱策略先跑它反而更贵；另外它在 4090 上只剩 2.3 GB 余量，是否接受需要你定。
- 2026-09-15 13:40（UTC），**审计要求的 `checkpoint save → 进程销毁 → strict reload → 继续 step` 工程门禁已实际执行并通过**。运行前先把上一轮状态文档提交为 `cdd9ba092d296458593009361dcb12011e4e18ce`（更早一次 parent 尝试在 `dirty=true` 下完成，已归档为 `attempts/checkpoint-gate-parent-dirty-worktree`，不作为验收证据）。协议清单 `manifests/depth-corruption-v3.checkpoint-gate.json`（SHA-256 `d4d741ace5f1549586bc0bfebb1e194284eafe92f036c9c1c60a69b65e418db9`；corruption 身份、`phase=qualification`、`run_kind=qualification`、epochs `3`、batch size `10`、`checkpoint_policy` 为 top-3 + latest + `earlier_epoch`）。三段轨迹：**parent** 在第 1 个 epoch 边界受控停止（`completed_epoch=1`、`global_optimizer_step=120`、best mIoU `2.02`，写出 `latest.pth`、`selector-epoch-1.pth`、`best-val-miou.pth` 与 `checkpoint-candidates.json`；`latest.pth` SHA-256 `157e7be55786a950c58ee52244fafbfecbe32c0532b87631210b645ffa4c8c23`）；**child** 以独立新进程从该存档 strict reload（`--resume` + `--resume-parent-run-id` + `--resume-checkpoint-sha256`）续跑到第 2 个 epoch（`completed_epoch=2`、`next_epoch=3`、`global_optimizer_step=247`、best mIoU `2.42`）；**reference** 为不中断的两 epoch 参照轨迹。**判定：** `compare-child-vs-reference.json` 在只忽略协议 `run_id` 的条件下 `pass=true`、`mismatches=[]`；model / optimizer / amp_scaler / rng_state 四个 component SHA-256 两侧逐位相同（`dc7780f90508f84e01d19541924969d5f60d00e378addf32e470b022a807760e`、`eb5362703b515739fd45f7906afb99d8cc59b81afe08948cb6d044d32ea68b84`、`321600b0d66d229dd860bef026a7c11c16b7d2640923cc9196594cdc0d855ff9`、`d2c4cb2f4d331176a7d11a37249c70a6e3ac28416d4e834a1f4d4a960100c58f`），epoch、step、LR、best、split 与 protocol 字段也全部一致；`inspect`/`compare` 产物 SHA-256 为 `14e700901f6a3a3e705af7fa0b953952cf7940423275723967f068bfcf4b5db4`（parent）、`ab61c362e1eaf85d517c6d6dfdd544769dfe3797f82d85f3174d2a85d477b2ef`（child）、`6c2cf15d99e9fa87eae494b94784f8c081ca332efe35a2d2c970d46950dd3de4`（reference）、`e0189aa5178692b9c6627676e6892fc919db8612ec271b87c61ab828adf0ce4c`（compare）；三段 `training_result.json` SHA-256 为 `ac6b0f4df7c3c80bd2ba4522425119217e22886115e5b7fbca13cd8167efbde1`（parent）、`9231371b34b5eddbff361afc78977201bbe14e40993071f21e059aa4cbe75a9b`（child）、`90614c4c0d65bcae8a2453ecc9a6a7b61fae294ed47666fb58241f962cf92495`（reference）。**同批次实测的显存事实：** corruption 身份在 batch size `10` 下经过第 1 个 epoch 的 validation 之后，第 2 个 epoch 的首个训练步只剩 `1.73 GiB` 自由显存，低于历史 `2 GiB` / `0.10` 安全余量（**不是 OOM**）；child 与 reference 在启用该安全闸时都因此中止，分别归档为 `attempts/checkpoint-gate-child-attempt1-vram-gate` 与 `attempts/checkpoint-gate-reference-attempt1-vram-gate`，恢复保真度验证改用关闭安全闸的方式完成。**仍未完成的 probe 边界项：** 控制面停机流程验证（本实例上没有 `compshare` CLI 与凭据，需要用户侧执行或提供）与 SwanLab `online` 模式（`/root/.config/dformer/swanlab.env` 缺失，probe 与门禁均使用 `offline`）。大白话说，存档、杀进程、重载、继续训练这条链已用"中断续跑与不中断两条轨迹逐位一致"证明可信；但同时也实测到验证阶段之后显存余量掉到 1.73 GB，24 GB 的 4090 跑 batch size 10 属于"能跑但余量偏紧"，是否接受需要你定。
- 2026-09-15 14:05（UTC），**本轮云端轮次的文档交付已完成，供上级模型决策**：新增 `liu-test-exp/方案1/改动细节3-云服务器.md`（云端实例与环境事实、数据集与 pretrained 逐位核验、三份协议清单与 preflight、两个身份的容量/吞吐实测、显存安全闸两次中止与放宽、checkpoint save→strict reload→继续 step 门禁、两条新实测事实、成本外推、one-arm screening 执行计划、失败归档与准确恢复点）；`liu-test-exp/方案1/MMFR-多形式模态失效可靠性学习-独立审计总规划.md` 已同步（第 0、1.1、1.3、7、12.2、12.4、13.3、13.4、14.4、16、17.3、18、19、20、21、22、24.2 节，含新增的 `MMFR-A2-v3-exploratory-one-arm-screening` 阶段与更新后的恢复点）；`protocols/mmfr-a2-train-integration-v3.template.json` 的 `status` 更新为 `cloud-probe-and-checkpoint-gate-passed-awaiting-one-arm-screening-authorization`，`authorization` 增加 `cloud_capacity_probe_completed` 与 `checkpoint_save_reload_continue_step_gate_completed`，`verification.not_run` 更新，并新增 `execution_plan_revision` 块（`MMFR-A2-v3-execution-plan-two-stage-cost-screening-v1`）；更新后原始字节 SHA-256 为 `b26d1e933ecb3c587de3313bd1a463a4d95044d0efa62ffb168f164324d52d6a`，`python -m json.tool` 校验通过，科学字段（`code_identity`、`qualification_tool_identity`、`data`、`training`、`a1_corruption_basis`、`corruption_application`、`depth_validity_and_supervision`、`telemetry`、`selector`、`evaluation`、`qualification_evidence`）未改；`doc/main/MUSeg-open-decisions.md` 第 13 节已同步该云端事实与 one-arm screening 处置。该交付不涉及任何新的 GPU、训练或云资源操作。大白话说，这一轮在云服务器上做了什么、量到什么、发现什么，以及新的"先跑一段省一半钱"的执行顺序，都已经写成上级模型可以直接审计的文件。
- **v2 证据：** 仓库外 `cloud/DVC-A1-valdev-boundary-zero-v2/`；修正后 protocol SHA-256 为 `3c6f33562692c8baee85786261de431583e9bdd9b6f8f51cd5ad0f042406f6bb`，allowlist SHA-256 仍为 `5589eb3378ed2e23180f6205e2d88cea39702ad4bfd5d4e1b739cf2f920a8d89`，allowlist 摘要 SHA-256 仍为 `f451955d91143d118f4445181ceb8ff7212ced5ba8cf2add12981cd1e75287c2`，修正后 preflight SHA-256 为 `95afc2f071656569a7f3ee18d3244c5f0abfc5ef59c349825e170ccc314c0e9d`。完整运行记录为 `cloud/DVC-A1-valdev-boundary-zero-v2/executions/20260908T143400225007+0000-full.json`，失败记录为 `cloud/DVC-A1-valdev-boundary-zero-v2/full-failure.json`；均记录 `official_test_included=false`。五个 condition JSON 均已写出，每个包含 218 样本/138 组，但统计因 `dose_effect` 只有 137 个有效配对组而阻塞。无效组定义诊断见 `doc/reports/2026-09-09-museg-dvc-a1-v2-group-definition-diagnosis.md`：4 张图的 29 像素 ignore 安全区均为空，三类原始前景在计分安全区中均被排除，故 15 类全部双空 `None`。组级诊断报告见 `doc/reports/2026-09-09-museg-dvc-a1-v2-protocol-blocked.md`；物化/preflight 历史报告见 `doc/reports/2026-09-08-museg-dvc-a1-v2-materialization-preflight.md`，保护修正报告见 `doc/reports/2026-09-08-museg-dvc-a1-v2-protection-fix.md`，修正后 preflight 报告见 `doc/reports/2026-09-08-museg-dvc-a1-v2-protection-preflight.md`。
- **v1 证据：** 日期化报告为 `doc/reports/2026-09-08-museg-dvc-a1-protocol-gate.md`；仓库外权威运行证据位于 `cloud/DVC-A1-valdev-boundary-zero-v1/attempt-2/`。最终 protocol SHA-256 为 `7bca3c109905d7d4ed228359bf8cd2a20bee18cbcd3d4cbdfed871e09870fba2`，preflight SHA-256 为 `aeb0afff829fc5abcd86196cb6c27822055b90c7910711c2da956a2133dc0c45`，mask manifest SHA-256 为 `60b988b3f9ffaabc5f6540cfccda48ddb5efd4d44ce360691bfaeea047e63f29`，failure SHA-256 为 `b36d9bddfb33a4f69657dae976f94afa6e97282a42aec27663949502be08e216`；均记录 `official_test_included=false`。
- **v3 证据：** 定义门禁证据位于仓库外 `cloud/DVC-A1-valdev-boundary-zero-v3-bgcontext/`；完整评价证据位于 `cloud/DVC-A1-valdev-boundary-zero-v3-bgcontext-full-20260909/`。定义门禁 protocol SHA-256 为 `f9960904f51cec11797ada6952c2102da4b2b6832d0bf7b529898bfae9c0f216`，CPU 标签域审计 SHA-256 为 `17a4ec36ba231c3be6ea1ed1f4f6e3b9f8380d8d0a619cc3530a6bfd903ad1db`，原 preflight SHA-256 为 `829b580b6ed4ace977cf578e8391bcc759fc6d135fad72c2bd0dba958712dedd`。完整评价 protocol SHA-256 为 `d52b3dba2c7a34894b9f4cdf1d8e313e304415d1ea821a191b7753b97be74f5d`，mask manifest SHA-256 为 `3da28ae84806c61b0178c9563e811eed9e6f16e3f0f4040ab5753b26abdc9e79`，summary SHA-256 为 `951d8e2005a4c64ac809ffd3de9d7db367df955a46e81c46e89516bd64d9f220`。完整运行退出码为 `0`，状态 `completed`，时长 `5507.055` 秒；主分析 `dose_effect` 为 `+0.0731348717` 个百分点（95% 区间 `[-0.0620441424,+0.2226503089]`），`specificity_effect` 为 `+0.0323919541` 个百分点（95% 区间 `[-0.3002343847,+0.2899672010]`），两者均为 `138/138` 有效配对组，主裁决为 `not-supported`。完整运行记录 `official_test_included=false`。
- A2/B2 深度有效性方向已迁入 `doc/plans/deferred/2026-09-MUSeg-unexecuted/MUSeg-A2-B2深度有效性/`，状态为**延期、未执行、未授权、当前不处于恢复点**。
- 方向1后验校准与 Depth 退化双路径计划已迁入 `doc/plans/deferred/2026-09-MUSeg-unexecuted/MUSeg-方向1最短验证路径/`，状态同样为**延期、未执行、未授权、当前不处于恢复点**。
- 两套延期计划只保留未来重新启用时的候选设计。重新启用必须先从稳定基准建立独立研究分支，重新确认数据、protocol、evaluator 和授权；计划中的“当前任务”“下一步”或“恢复点”不构成执行依据。
- 方向1仍需使用的研究设计提示词和论文编号索引已经随计划收进其 `参考资料/` 与 `补充内容/`，不再作为活跃草稿目录入口。

## 3. 数据、评估和 official-test 边界

- B0、A2/B2 候选方向和未来模块消融都必须显式绑定 checkpoint、split、输入契约、metric geometry、evaluator、protocol 和产物哈希。
- `val-dev` 已参与 B0 checkpoint 选择；它不能在没有新数据职责和新 protocol 的情况下直接改作方向1的独立 calibration/evaluation 集。
- A2/B2 若未来重新启用，只能先在 `val-dev` 上按新 protocol 处理；人工 corruption 证据与自然无效深度证据的外推边界按 `MUSeg-open-decisions.md` 执行。
- official test 在模型选择、阈值冻结、方向筛选、开发评估和恢复流程中继续保持 `sealed_unread`；任何解封都需要独立门禁和单独授权。
- 本轮历史上已完成 DVC-A1 v1–v3 与 DVG-B1 P1–P4 的相应门禁和评价；MMFR v1、v2 均在正式训练前归档。当前身份 `MMFR-A2-train-integration-v3` 已完成 A + MID-A 语义接入和五项本地资格：severity/burden、CPU qualification（`121` 项）、validity transport（`48` 项）、initial-state equivalence（`714` 个共有参数与 `88` 个共有 buffer 逐位相同）以及 batch size `10` 的 AMP update-path isolation（`17` 次尝试、`10` 次成功更新）。当前仍没有 v3 正式训练 checkpoint 或效果指标；云端容量/吞吐 probe、完整 evaluator、云资源操作和 official test 均未运行、未授权。

## 4. 证据位置与历史解释

- 2026-09-15 的 MMFR 当前协议入口为 `protocols/mmfr-a2-train-integration-v3.template.json`；高级审计原文与执行回填位于 `liu-test-exp/方案1/改动说明.md`。v1 模板保持归档，v2 模板、commit `3c8ebddeb76e63a5be37e261381117abfba117ca` 与 tag `MMFR-A2-v2-pretrain-freeze` 保留为历史；v3 源码、config、五个资格工具、五份 canonical 报告及 raw/LF 双 SHA-256 均由 v3 protocol 统一索引。当前准确恢复点是：状态已达到 `eligible-for-cloud-capacity-probe-authorization`，下一项开放决定仅是用户是否单独授权云端单 GPU、batch size 10 容量/吞吐短 probe；若 probe 再通过，仍需用户另行授权 `MMFR-A2-clean-control-v3` 与 `MMFR-A2-depth-corruption-train-v3` 两个公平对照正式训练。A + MID-A、natural-invalid 权重不变、8 个全无有效 Depth crop 保留及 43 个 optimizer-missing 参数不修均已冻结，不再是当前开放项。完整训练、checkpoint 效果评价、完整 evaluator、official test 以及本次创建之外的任何云资源操作均未授权（本次实例创建与停机已由用户单独授权并完成）。 2026-09-15 18:39（UTC+8）追加事实：`cpod-1vbh7faqcauq`（`mmfr-a2-4090-probe`，`cn-bj2-03`，RTX 4090 一张、14 vCPU、32 GiB、50 GiB 启动盘、镜像 `pytorch_2.1.2_Py3.10`）已创建并在约 `13` 秒后停机，状态 `Stopped`；当前恢复点是等用户确认该实例配置正确，之后才谈 probe 执行授权。2026-09-15 12:10（UTC）追加事实：该实例已由用户重新启动并处于无卡模式，数据集 `MUSeg_DFormer` 已完成落地与逐位核验（详见第 2 节同时间条目），归档 `MUSeg_DFormer.7z` 已按用户确认删除、被核验的数据目录保留；恢复点已于 2026-09-15 13:40（UTC）更新为：`v3 scientific protocol frozen; local qualifications PASS; cloud batch-10 capacity/throughput probe COMPLETED for both identities; checkpoint save / strict-reload / continue-step engineering gate PASS (resumed child == uninterrupted reference on model, optimizer, amp_scaler and rng_state component hashes); next sequence = control-plane stop-flow verification + SwanLab online credentials + user decision on the batch-10 VRAM margin -> user authorization for one-arm MMFR-A2-depth-corruption-train-v3 500e screening -> senior screening review -> conditional clean-control-v3 500e authorization -> formal paired evaluation`。probe 之后仍未授权的事项：两个 500 epoch 训练（其中 one-arm screening 本身也需要用户单独授权）、完整 evaluator、checkpoint 效果评价、official test，以及本次 probe 之外的任何云资源操作。待用户决定的紧邻事项：batch size `10` 在 4090 上 corruption 侧仅剩约 `2.33 GiB`（`9.67%`）余量的处置方式，以及 SwanLab `online` 凭据（`/root/.config/dformer/swanlab.env` 在本实例缺失，需要用户提供或自行写入）。此前 DVG-B1 P0–P4 与 `oracle-not-supported` 历史收口保持不变。大白话说，容量和速度已经量完，接下来先补"存档-重载-停机"三道工程检查，再等你单独批准只跑"深度损坏"那一个 500 epoch 的省钱筛选。
- 2026-09-15 的 post-freeze 文档收口已完成：`liu-test-exp/方案1/MMFR-多形式模态失效可靠性学习-独立审计总规划.md` 已从等待高级审计的 v2 快照更新为 v3 当前审计版，新建 `liu-test-exp/方案1/改动细节2.md` 记录 v2→v3 逐文件改动、五项资格证据、raw/LF 哈希、授权边界和准确恢复点；原 `改动细节.md` 继续作为 v1→v2 历史快照。这两项是文档证据入口更新，不改变 commit `4ce7b67c5707991750461ef2f806e12b37e203f9` / tag `MMFR-A2-v3-pretrain-freeze` 固定的科学 source identity，也不代表运行了新的资格、GPU、训练或云任务。大白话说，外部审计材料现在已经跟上 v3 当前事实，但实验状态没有因此前进。
- 2026-09-10 已生成覆盖 2026-08-17 至 2026-09-10 的组会正式报告 `doc/reports/2026-09-10-museg-dformerv2-to-mve-group-meeting.md`，并生成、发布版本化 Canvas `doc/canvases/0.0.13-museg-dformerv2-to-mve-group-meeting.canvas.tsx`；两者已登记到 `doc/reports/report-index.json`，下一个 Canvas 版本为 `0.0.14`。大白话说，明日组会需要的长报告和可视化展示版已经落地，但本次没有产生新的训练、评价或科学裁决。
- 正式报告和 Canvas 元数据入口：`doc/reports/report-index.json`。
- 历史计划入口：`doc/plans/`；已完成 Quick-B0 与更早阶段计划位于 `doc/plans/archive/`。
- 未执行候选计划入口：`doc/plans/deferred/2026-09-MUSeg-unexecuted/README.md`。
- 稳定项目指南：`doc/guides/project/README.md`；当前文件职责索引：`doc/guides/project/file-catalog.md`；完整旧目录作为日期化历史快照保留在同目录。
- 2026-09-05 上下文归档清单：`doc/archive-manifests/2026-09-05-context-cleanup.md`；归档 ZIP 在本地 `archive-export/`，已被 `.gitignore` 排除。
- 历史报告中的“正在运行”“当前计划”或“待闭合”只按报告形成时点理解；若报告正文没有后继指针，以本文件和报告索引为准，不把历史措辞当作当前事实。

## 5. Git 基准与治理状态

- **整理状态：** 已完成本轮文档、归档清单、目录迁移、引用治理和 Git 元数据治理；整理提交为 `de731aa611db1ba3a6511153c29f7e568f91da09`，随后在 `main` 上追加 Cursor skill 提交 `be07e96390a75b1d7651c1bf29979153d7f74a49`。本地 annotated tag `museg-research-base-v1` 当前指向 `main` 的最新提交，远端不推送。
- **2026-09-15 Git 状态：** 独立分支 `research/multimodal-failure-reliability` 保留 v2 freeze commit `3c8ebddeb76e63a5be37e261381117abfba117ca` 与 annotated tag `MMFR-A2-v2-pretrain-freeze`；本批次以新的 annotated tag `MMFR-A2-v3-pretrain-freeze` 指向 v3 收口提交，固定生产接入、三个 v3 config、v3 protocol、五个资格工具与状态记录。`outputs/` 继续由 Git 忽略，仅通过 protocol 中的 canonical 路径与 raw/LF SHA-256 引用。没有覆盖旧 tag，没有推送远端。
- **大白话说明：** v2 和 v3 各自有独立的本地冻结标签，历史可回查；远端没有变化。下一步若要创建云实例，仍必须取得用户的单独授权。
- v3 冻结提交完成后工作区应保持 clean；任何后续改动必须重新检查 source identity。未经用户确认不得推送远端。
- 稳定基准治理指南：`doc/guides/project/research-branch-governance.md`。全新论文方向默认从稳定基准创建；若像 MMFR 一样明确继承已收口方向的必要代码，则必须记录准确起点，并重新建立独立语义名称、config、protocol 和证据身份。
- Git 整理完成后，后续方向不得从 B0 最终 checkpoint 续训后冒充公平消融；必须从同一官方 pretrained 独立训练，并建立新的 config、protocol 和证据身份。

## 6. 恢复规则

1. 每个 MUSeg 对话先读取本文件；涉及研究选择时再读取 `doc/main/MUSeg-open-decisions.md`。
2. 先确认本文件中的当前授权边界和恢复点；延期计划、历史报告和旧交接材料不能单独触发恢复。
3. 新方向先建立独立 `research/<topic>` 分支，冻结 protocol/config/evaluator/split 身份，再按具体操作取得授权。
4. 任何训练、checkpoint、指标、official-test、云实例、证据位置、阻塞项、恢复步骤、提交或发布状态变化，都必须在最终答复前更新本文件。
5. 纯文档或只读检查没有持久事实变化时，不更新时间；历史指标和原始 acceptance/result 文件不得为匹配新口径而改写。
