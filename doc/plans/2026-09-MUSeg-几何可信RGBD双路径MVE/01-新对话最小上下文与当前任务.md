# MUSeg 几何可信 RGB-D 双路径 MVE：新对话最小上下文与当前任务

> **文档角色：** 新对话最小上下文；只描述本计划的资料补充阶段，不替代项目实时状态。
> **计划状态：** 待规划（资料补充中）；没有已授权的实验执行项。
> **形成或核验时点：** 2026-09-07。
> **实时入口：** [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)。
> **研究选择：** [`MUSeg-open-decisions.md`](../../main/MUSeg-open-decisions.md)。
> **上级方向：** [`00-总方向规划.md`](./00-总方向规划.md)。
> **当前任务：** 只补充并核对 [`参考资料/00-待补充论文内容清单.md`](./参考资料/00-待补充论文内容清单.md)；没有实验子计划处于执行或恢复点。

## 1. 新对话必读顺序

1. [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)：整个项目的当前事实、授权和恢复规则。
2. 本文件：本计划当前走到哪里。
3. [`00-总方向规划.md`](./00-总方向规划.md)：双路径目标、固定边界和后续顺序。
4. 涉及实验口径时读取 [`MUSeg-open-decisions.md`](../../main/MUSeg-open-decisions.md)。

## 2. 计划内当前状态

- 已完成：核对原始双路径草案、两份论文题录清单、MUSeg 实时状态与开放决策；建立新计划目录和资料补充门禁。
- 未完成：共享 protocol、`DVC-A1` 问题验证子计划、`DVG-B1` 方案验证子计划及任何实现/实验。
- 当前阻塞：关键论文只有摘要；原草案引用的 `RE447` 不在 WOS 原清单；三维相机参数、严格 RGB-only 输出和新 `C/E` 数据职责尚未由项目事实核验。
- 准确恢复点：用户将资料填入 [`参考资料/00-待补充论文内容清单.md`](./参考资料/00-待补充论文内容清单.md) 指定位置或按清单给出本地 Markdown 文件后，先做来源与内容完整性核对。
- 后继候选：资料闭合后新增首个详细子计划 `DVC-A1`；文件名和 protocol 后缀届时按冻结数据职责确定。

## 3. 最少需要知道的固定身份

- 稳定起点是 DFormerv2-S RGB Quick-B0，epoch 420 checkpoint SHA-256 为 `f246a3afc50334c81302b7bfebdadf7cf37d00326bf1c3aa54f6a151754e3a1c`。
- `val-dev` 有 318 条且已参与 checkpoint 选择，不能直接作为新的独立评价集 `E`。
- official test 为 `sealed_unread`，本计划不得读取或用于任何选择。
- 现有基线是 RGB-D 模型；严格可比的 RGB-only 输出目前未核验，不能把 RGB-D 模型的置零 Depth 输入称为严格 RGB-only 模型。
- 相机内参、外参和深度尺度未核验前，三维点到表面指标只能作为条件分支，不能作为硬性 MVE 成功门槛。

## 4. 本次原拟议任务卡

- **唯一交付物：** 用户补齐清单中“必须补充”的论文方法/实验片段，并说明 `RE447` 的处置。
- **输入：** 原始双路径草案、CVPR/WOS 题录清单和用户补充的 Markdown 内容。
- **输出：** 可逐项核验的论文片段与来源信息；本阶段不输出已冻结实验协议。
- **前置门禁：** 每篇材料至少包含题名、来源/链接或 DOI、所摘章节名，并保留公式、表格和定义所需上下文。
- **停止条件：** 必须项任一仍缺失或来源身份不清时，保持 `reference-blocked`；不根据摘要补写公式、实现参数或论文结论。

## 5. 按需读取与禁止范围

资料补充阶段只需定点读取本目录、原始双路径草案和两份题录清单。暂不全仓搜索，不读取大型日志、预测、checkpoint 或 MUSeg 图像数据，也不扫描 official test。

资料闭合后，下一对话才定点核对：

- MUSeg 本地目录是否提供场景/矿区分组和相机参数；
- 当前代码的 Depth16 无效值、量化与 evaluator 链；
- 是否存在同标签空间、同数据职责和可复核 checkpoint 的严格 RGB-only 模型；
- 现有 `tools/mve` 历史脚本哪些只能作为旧 pilot 参考，不能直接复用。

## 6. 验证与状态更新预算

本阶段是纯文档工作，只检查内容、相对链接和差异；不运行项目测试、GPU、训练、长耗时评价、云资源或 official test。只有后续产生新的实验事实、授权边界或恢复点变化时，才更新 `MUSeg-current-status.md`。

## 7. 可直接粘贴给新对话的短提示词

```text
继续 doc/plans/2026-09-MUSeg-几何可信RGBD双路径MVE。先读 doc/main/MUSeg-current-status.md、本目录 01 和 00；再核对参考资料/00-待补充论文内容清单.md 中我补充的内容。只完成资料完整性与引用边界检查；若“必须补充”项闭合，再规划共享 protocol 和第一个 DVC-A1 子计划，不执行代码、数据扫描、GPU、训练或 official test。
```
