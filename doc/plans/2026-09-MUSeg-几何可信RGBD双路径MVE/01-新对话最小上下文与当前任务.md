# MUSeg 几何可信 RGB-D 双路径 MVE：新对话最小上下文与当前任务

> **文档角色：** 新对话最小上下文；只描述本方向形成时点的恢复入口，不替代项目实时状态。
> **计划状态：** `DVC-A1-valdev-boundary-zero-v3-bgcontext` 与 `DVG-B1-oracle-gsa-v1` 均已完成并裁决为 `not-supported`/`oracle-not-supported`。DVG-B1 的 P0–P4 已全部闭合；P4 在 218 张图、138 个 location group、五个 condition、每图 10 view 的本地 GPU 配对开发评价中未达到预注册收益门槛。当前恢复点是回到方向级研究选择，不在本 protocol 下追加规则或运行。
> **形成或核验时点：** 2026-09-13 15:05 UTC。
> **实时入口：** [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)。
> **研究选择：** [`MUSeg-open-decisions.md`](../../main/MUSeg-open-decisions.md)。
> **上级方向：** [`00-总方向规划.md`](./00-总方向规划.md)。
> **当前执行计划：** [`04-DVG-B1条件式Oracle门控.md`](./04-DVG-B1条件式Oracle门控.md)。

## 1. 新对话必读顺序

1. [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)：整个项目的当前事实、授权边界和恢复点。
2. 本文件：本方向走到哪里，以及新对话只做什么。
3. [`00-总方向规划.md`](./00-总方向规划.md)：双路径目标、固定身份和子计划关系。
4. 涉及阈值、裁决或实验口径时读取 [`MUSeg-open-decisions.md`](../../main/MUSeg-open-decisions.md) 第 12 节。
5. 执行 DVG-B1 时读取 [`04-DVG-B1条件式Oracle门控.md`](./04-DVG-B1条件式Oracle门控.md) 第 6、8、10 节。
6. 只需核对文献/官方代码为何不能唯一决定 A/B 时，读取 [`DVG-B1-必须实现细节靶向检索步骤与WOS检索式.md`](../../../liu-test-exp/方案1/DVG-B1-必须实现细节靶向检索步骤与WOS检索式.md) 第 6.7 和第 8 节；不再重复通读 11 篇全文或 8 个常见仓库。

若任何计划文字与实时状态冲突，以 `MUSeg-current-status.md` 为准。

## 2. 计划内当前状态

- `DVC-A1-valdev-boundary-zero-v1` 与 v2 保持各自 `protocol-blocked` 历史终态；不得覆盖或重解释。
- 独立 v3 已完成 218 张图、138 个 location group 的完整开发评价，主裁决为 `not-supported`；该结论不阻止单独验证 Oracle 门控方案，但 B 的结果不能回写成 A 的支持证据。
- 11 篇全文和常见官方代码核验已完成。它们能支撑连续 confidence、可靠性乘 affinity、Full attention 等机制边界，却没有唯一给出 DFormerv2 所需的四级 reliability 和 Full/H/W gate。
- 用户已选择 A：原始 `m=1` 表示受损、`r=1-m` 表示可信；先按 evaluator 用 `INTER_LINEAR` 从原图映射到五尺度 view，再 flip、右/下 padding `r=1`，最后对四个 stage 用 `INTER_AREA` 聚合为连续有效面积比例。
- 用户已选择 B：Full/H/W 均使用 query/key 两端 reliability 的连续对称乘积，只乘 `self.weight[1] * mask_d*`；spatial contribution、Q/K/V、rotary encoding、Depth 输入、decoder 和 logits 不变。
- `None`、clean、q=0、全可信 mask 必须进入原始 forward 旁路；逐 stage 输出和最终 pre-softmax logits 以 `torch.equal` 验收，不使用 `allclose` 放宽。
- C 已关闭：`oracle-supported` 要求 Boundary IoU 点估计至少 `+0.10` 个百分点、双侧 95% percentile interval 下界严格大于 `0`，且 mIoU 点估计不得为负。该规则是项目预注册选择，不是文献标准。
- P0 已完成：模板为 `protocols/dvg-b1-oracle-gsa-v1.template.json`，物化器为 `tools/mve/dvg_b1_protocol.py`；仓库外 protocol 为 `cloud/DVG-B1-oracle-gsa-v1/protocol.json`，SHA-256 为 `e7b9ed0a3c84053736f70a7807bdd4f270ee5bf84a6b85ca5cfd053f9cc47e46`，并已生成 `allowlist-summary.json`。
- P1 已完成：最新 `qualification.json` SHA-256 为 `e101c972a25ada6749e7d8408172938d104ecfb4eee9266f1fbab7fc632b3dc3`，160 个 view-stage 的 OpenCV `INTER_AREA` 与独立精确块均值参考一致，几何守卫和 Full/H/W gate 检查通过；未加载 checkpoint、未执行 forward、未使用 GPU。
- P2 已完成：本地 RTX 5060 Laptop GPU 严格加载 epoch 420 checkpoint，`None`、clean、q=0、全可信 mask 的四级输出与最终 logits 在配对 RNG 后全部 `torch.equal`；产物 SHA-256 为 `a9cdbe8ca3b0ef210184499a06702103e71855e8b3529472403aae00fe31df24`。
- P3 已完成并经强化重跑：确定性选择 v3 allowlist 样本 `02-01-01-0283-240524103235-08-99`，clean、非空 `boundary-q75`、`nonboundary-q50` 各覆盖 10 view；20 个受损 view 的 Depth delta 均非空且不越出变换后的 mask support，80 个 baseline 与 80 个 gated 四级首块 GSA 审计分别通过 raw-depth 与 gated-depth 生产路径重建，paired RNG、有限值和 `932×1082` 原图网格均已记录。最终 canonical SHA-256 为 `bd2ec0f677b89a5a3129c1823c876b5b510262f28d8d96499d023af826c1360c`。
- 首个结构化 P2 尝试因 Ham decoder 的随机 NMF bases 导致最终 logits 重采样而阻塞，失败产物已归档；四级 backbone 当时已全部严格相等。最终验收只回放相同 RNG 以隔离接口变量，没有修改模型或放宽容差。
- P4 已完成：218 张图、138 个 location group、五个 condition、每图 10 view，共 `10,900` 个 baseline/gated 配对 forward pair。clean 的所有 view 和融合输出严格相等；mask/source hash、q=0 Depth8、Depth delta 不越出 mask、有限值、原图网格、配对 RNG 和 138 组统计均通过。
- 主条件 `boundary-q75` 的 Oracle-minus-baseline Boundary IoU 为 `-0.1508352015` 个百分点，95% interval `[-0.2676580460,-0.0462325573]`；mIoU 为 `-0.2316731726` 个百分点，95% interval `[-0.3914881430,-0.0919002976]`。最终裁决为 `oracle-not-supported`。
- P4 canonical `full-evaluation.json` SHA-256 为 `f5cadf93ace37868b27ecef2f5a96c18702ba821b8eda242f3c7921982a42f12`；当前没有后续 DVG-B1 运行任务，训练、云资源和 official test 未授权。

## 3. 最少需要知道的固定身份

- **模型：** DFormerv2-S RGB Quick-B0，epoch 420 checkpoint SHA-256 为 `f246a3afc50334c81302b7bfebdadf7cf37d00326bf1c3aa54f6a151754e3a1c`。
- **数据：** B1 复用 v3 的 218 张图、138 个 location group；`val-dev` 已参与 checkpoint 选择，因此结果只能称为 paired development evidence，不能称为独立测试。
- **输入：** corruption 在原始二维 `uint16 Depth16` 上生成并置零，之后走现有量化、归一化和 evaluator；Oracle mask 必须与同一 condition 的 corruption mask 和哈希一致。
- **evaluator：** `msflip-whole-original-grid-v1`，五尺度 × 原图/水平翻转，共 10 view；只在右/下 padding，逆 flip 后恢复原始 Label 网格，FP32 平均 pre-softmax logits。
- **指标与统计：** Boundary IoU 主指标、mIoU 辅助指标；以 138 个 location group 为 bootstrap 单位，seed `20260908`，10,000 次 percentile bootstrap。
- **official test：** 继续 `sealed_unread`，所有 protocol/产物必须记录 `official_test_included=false`。

## 4. 新对话的唯一任务卡

### 4.1 当前交付物：DVG-B1 收口

- **完成身份：** P0 protocol、P1 qualification、P2 no-op、P3 gate preflight 与 P4 full evaluation 均已直接核验；P4 `full-evaluation.json` SHA-256 为 `f5cadf93ace37868b27ecef2f5a96c18702ba821b8eda242f3c7921982a42f12`。
- **科学裁决：** `boundary-q75` 的 Boundary IoU 与 mIoU 点估计均为负，按冻结 C 裁决为 `oracle-not-supported`。
- **解释边界：** 该结果关闭当前 Oracle GSA 门控方案，不回写成 DVC-A1 的支持证据，也不自动授权质量预测、恢复网络、训练、云资源或 official test。
- **准确恢复点：** 回到 `00-总方向规划.md` 选择新的独立候选；若改变 A/B/C 或重新评价，必须建立新 protocol identity，不得在 v1 下追加结果导向的候选。

### 4.2 已完成顺序

1. **P0–P3：** 已完成并通过实现与前置门禁。
2. **P4 完整评价：** 已完成并形成 `oracle-not-supported` 合法终点。

本 protocol 已收口；不得查看结果后换成 nearest、min、query-only、hard threshold、stage 子集，或追加 condition、阈值、样本和 seed。

## 5. 按需读取范围

代码实现只定点读取：

- `models/encoders/DFormerv2.py`：`GeoPriorGen`、Decomposed/Full GSA、四级调用链；
- `models/builder.py`：`EncoderDecoder.forward/encode_decode` 参数入口；
- `tools/evaluate_museg_checkpoint.py`：`build_msflip_views` 与 logits 恢复链；
- `tools/mve/dvc_a1_core.py`、`tools/mve/run_dvc_a1.py`、`tools/mve/dvc_a1_protocol.py`：corruption、preflight、protocol 和 execution record 的复用锚点；
- v3 冻结 protocol、allowlist、mask manifest 和 summary 的明确路径。

默认禁止全仓扫描、大型预测/checkpoint 内容读取、重复全文检索、重复克隆第三方仓库、official test、训练、云资源和未授权的完整 GPU 评价。

## 6. 验证与状态更新预算

- 纯文档阶段只做内容、相对链接和差异检查，不运行项目测试。
- 代码阶段只做覆盖当前门禁的最小定点检查；不主动生成临时 `test_*.py` 或一次性验证脚本。若需要新增或大幅扩展永久测试文件，先取得用户确认。
- GPU preflight、完整评价、训练、云任务和 official test 都是不同授权层级，前一步通过不自动授权后一步。
- protocol、阻塞项、恢复点、指标、checkpoint、证据位置或授权状态变化时，在最终答复前更新 [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)；研究选择同步到开放决策。

## 7. 可直接粘贴给新对话的提示词

```text
继续执行 `doc/plans/2026-09-MUSeg-几何可信RGBD双路径MVE/04-DVG-B1条件式Oracle门控.md`。先按顺序读取 `doc/main/MUSeg-current-status.md`、本目录 `01-新对话最小上下文与当前任务.md`、`00-总方向规划.md`、`04-DVG-B1条件式Oracle门控.md`，涉及裁决时再读 `doc/main/MUSeg-open-decisions.md` 第 12 节；不再重复全文或常见仓库检索。

A/B/C 已冻结并物化为 `DVG-B1-oracle-gsa-v1` P0 protocol。A 使用 `m=1` 受损、`r=1-m` 可信，原图 reliability 按 evaluator 先用 `INTER_LINEAR` resize 到五尺度 view，再 flip、右/下 padding `r=1`，各 stage 用 `INTER_AREA` 得到连续有效面积比例；B 使用 query/key 两端 reliability 的对称乘积构造 Full `[B,1,L,L]`、H `[B,1,W,H,H]`、W `[B,1,H,W,W]` gate，只乘 depth geometry contribution。C 要求 `boundary-q75` 的 Boundary IoU 点估计至少 `+0.10` 个百分点、95% percentile interval 下界严格大于 `0`，且 mIoU 点估计不得为负。`None`、clean、q=0、全可信 mask 必须走原始 forward 旁路并用逐 stage/最终 logits `torch.equal` 验收。

直接核对 `cloud/DVG-B1-oracle-gsa-v1/protocol.json`、`qualification.json`、`noop-equivalence.json`、`gate-preflight.json` 和 `full-evaluation.json` 的 SHA-256。P0–P4 已完成；P4 的主条件 Boundary IoU 为 `-0.1508352015` 个百分点、95% interval `[-0.2676580460,-0.0462325573]`，mIoU 为 `-0.2316731726` 个百分点，裁决为 `oracle-not-supported`。当前只需从方向级计划选择新的独立候选，不得在 DVG-B1 v1 下修改 A/B/C 或追加评价。不得训练、使用云资源或读取 official test。
```
