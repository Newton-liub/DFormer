# MUSeg 几何可信 RGB-D 双路径 MVE：新对话最小上下文与当前任务

> **文档角色：** 新对话最小上下文；描述本计划形成时点的恢复入口，不替代项目实时状态。
> **计划状态：** 资料门禁已完成；`DVC-A1` 详细计划待用户批准实现，当前没有已授权实验。
> **形成或核验时点：** 2026-09-08。
> **实时入口：** [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)。
> **研究选择：** [`MUSeg-open-decisions.md`](../../main/MUSeg-open-decisions.md)。
> **上级方向：** [`00-总方向规划.md`](./00-总方向规划.md)。
> **当前任务：** 审批并按需执行 [`03-共享协议与DVC-A1问题验证.md`](./03-共享协议与DVC-A1问题验证.md)；未获批前只允许继续修改计划文档。

## 1. 新对话必读顺序

1. [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)：整个项目的当前事实、授权和恢复规则。
2. 本文件：本计划走到哪里。
3. [`00-总方向规划.md`](./00-总方向规划.md)：双路径目标、收缩裁决和子计划顺序。
4. 涉及实验口径时读取 [`MUSeg-open-decisions.md`](../../main/MUSeg-open-decisions.md)。

## 2. 计划内当前状态

- 已完成：[`02-文献补充与协议设计门禁.md`](./02-文献补充与协议设计门禁.md) 及 [`参考资料/00-待补充论文内容清单.md`](./参考资料/00-待补充论文内容清单.md) 的 P0/P1 定点核对。
- 已处置：`RE447` 不适合作统计来源；paired/cluster bootstrap 改为项目预注册的一般统计流程，不继续为该简单操作补文献。
- 已收缩：首轮删除三维评价、亮度引导补全、高置信阈值、严格 RGB-only 回退和可学习 logits 门控。
- 已创建：一个详细子计划 [`03-共享协议与DVC-A1问题验证.md`](./03-共享协议与DVC-A1问题验证.md)，以及一个条件式后继 [`04-DVG-B1条件式Oracle门控.md`](./04-DVG-B1条件式Oracle门控.md)。
- 未完成且未授权：任何代码实现、数据读取、mask 生成、GPU 推理、模型评价、训练或 official test 操作。
- 准确恢复点：用户审批 `03` 的范围；获批后先物化 protocol 和做最小 CPU 静态/preflight 检查，q=0 等价门禁通过后才能请求完整 `val-dev` GPU 评价授权。

## 3. 最少需要知道的固定身份

- 模型是 DFormerv2-S RGB Quick-B0，epoch 420 checkpoint SHA-256 为 `f246a3afc50334c81302b7bfebdadf7cf37d00326bf1c3aa54f6a151754e3a1c`。
- `val-dev` 有 318 条、196 个 location group，覆盖六个 mine，split SHA-256 为 `1d0719d8f64f016d48995c25ab66d4004d76b7155d9efeef7cbb7454c0dd0e83`；它已参与 checkpoint 选择，结果只能称为 development evidence。
- 原始 `Depth16` 是二维 `uint16`，`0` 为无效；现有模型输入使用固定映射 `round(Depth16 * 255 / 13932)` 形成 8-bit Depth。注入应发生在原始 Depth16 上，再统一走量化、归一化和 evaluator 链。
- 当前没有严格可比的 RGB-only checkpoint/logits；首轮不使用 RGB-only 回退。
- 主 evaluator 是 `msflip-whole-original-grid-v1`；新 MVE 必须另建 identity，但保持相同五尺度翻转和原始 Label 网格。
- official test 继续为 `sealed_unread`，本计划不得读取。

## 4. 本次原拟议任务卡

- **唯一交付物：** 在用户批准后，完成 `DVC-A1-valdev-boundary-zero-v1` 的 protocol 物化、最小实现和分阶段证据；GPU 完整评价需单独明确授权。
- **输入：** 固定 checkpoint、`val-dev`、原始 `Depth16`、RGB/Label、现有主 evaluator 和 Boundary IoU 定义。【文献依据：参考资料清单 P0-1、P0-5】
- **输出：** protocol/manifest、确定性 corruption mask 或可重建参数、q=0 等价证据、配对逐样本指标和 `supported/not-supported/inconclusive/protocol-blocked` 裁决。
- **前置门禁：** checkpoint/split/Depth 映射/label ignore/Boundary IoU 多类规则全部写入 protocol；`official_test_included=false`；不读取结果后改阈值。
- **停止条件：** 身份不符、q=0 不等价、非有限值、Boundary IoU 小例不符或用户尚未批准对应执行阶段时立即停止。

## 5. 按需读取与禁止范围

实现时只定点读取：

- `models/encoders/DFormerv2.py` 的 depth/GSA 路径；
- `utils/dataloader/` 的 Depth 读取和归一化；
- `tools/evaluate_museg_checkpoint.py` 的主 evaluator；
- `data/splits/MUSeg/dev-v1/` 的冻结 split identity；
- 历史 `tools/mve/a2_*` 只作反例/旧实现参考，不直接复用其 official-test 选样或 `boundary_band_mIoU`。

默认禁止全仓扫描、大型日志/预测/checkpoint 内容读取、official test、训练、云资源和未在当前阶段获批的 GPU 完整评价。

## 6. 验证与状态更新预算

- 计划文档只做内容、链接和差异检查，不运行项目测试。
- 实现阶段只做定点检查：mask 确定性与嵌套、q=0 byte/numeric 等价、Boundary IoU 手算小例、1–2 样本推理 preflight。
- 不主动新增大测试文件、全套测试或重复性 smoke tests。完整 `val-dev` GPU 评价、训练和云任务均需单独授权。
- protocol、阻塞项、恢复点、指标或 checkpoint 身份一旦发生持久变化，最终答复前更新 [`MUSeg-current-status.md`](../../main/MUSeg-current-status.md)；真正开放的研究选择同步写入开放决策。

## 7. 可直接粘贴给新对话的短提示词

```text
继续 doc/plans/2026-09-MUSeg-几何可信RGBD双路径MVE。先读 doc/main/MUSeg-current-status.md、本目录 01、00 和 03；确认当前授权。若只获批实现，则仅物化 DVC-A1-valdev-boundary-zero-v1 protocol、最小 corruption/Boundary IoU 代码和定点 preflight，不跑完整 val-dev、GPU、训练、云任务或 official test。若 q=0 或身份门禁失败，停止并记录准确恢复点。
```
