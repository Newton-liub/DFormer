---
## 【J2-1】研究方案设计专用提示词｜DFormerv2-S 后验校准方案（项目适配版）

# 项目适配说明／状态边界

> **核验时点：** 2026-08-31。
> **实时事实入口：** `doc/main/MUSeg-current-status.md`。若本文与实时入口冲突，以实时入口为准。
> **研究定位：** 本文设计“方向1”后验校准路线，不是当前 A2/B2 深度有效性阶段的执行方案，也不自动授权数据划分、代码实现、GPU、训练、云资源或 official test。

## 1. 本方案的研究对象

本方案面向当前 DFormer 项目的 MUSeg 研究，主研究对象固定为：

- **模型**：`DFormerv2-S + ham`；
- **任务**：MUSeg 15 类 RGB-D 语义分割；
- **固定基础模型**：已完成的 single-seed RGB development Quick-B0；
- **最终 checkpoint**：epoch 420 的 `selector-epoch-420.pth`，SHA-256 为 `f246a3afc50334c81302b7bfebdadf7cf37d00326bf1c3aa54f6a151754e3a1c`；
- **输入**：RGB 与对齐的 Depth。RGB 使用 `rgb-imagenet-rgb-order-v1`；Depth 使用当前全局量化 8-bit、单通道复制 3 通道、mean `0.48`、std `0.28` 的输入契约；
- **输出与计分空间**：15 类逐像素 logits，正式指标恢复到 MUSeg 原始 Label 网格计分；
- **几何边界**：`480×640` 是训练随机裁剪尺寸，不是统一 validation 或 metric 尺寸；冻结主 evaluator 为 `msflip-whole-original-grid-v1`；
- **当前 B0 主结果**：mIoU `58.79`、mAcc `69.91`、mF1 `72.73`，使用完整 318 条 `val-dev`，official test 未参与。

这里的 B0 是本项目用于工具开发、后验校准探索和初步方法消融的**固定内部基线**，不是 MUSeg 论文中 DFormer-S 数值的严格复现，也不是三随机种子的论文级统计结论。若未来需要严格复现论文或形成重要定量结论，必须另立配置、配对重复、训练与验收协议。

## 2. 状态边界

本文只做研究方案设计。Quick-B0 已经完成并冻结；后验校准实验、独立 calibration 集、logits 导出和校准工具尚未执行或实现。

### 现有能力与已确认事实

1. Quick-B0 已完成 500/500 epochs 和 4 个候选的五尺度翻转主评估；最终固定 epoch 420。三 seed 不再是方向1方案设计的前置条件，但这不自动授权校准工具实现、logits 导出、指标计算或实验运行。
2. `models/builder.py` 已能构建 `DFormerv2_S`，接收 RGB 与辅助模态输入，并将解码输出插值到输入空间尺寸；无标签推理时返回逐像素 logits。
3. `models/encoders/DFormerv2.py` 提供本项目 DFormerv2 系列 RGB-D 编码实现；当前 B0 保留 B1 `safe_masked_mean` 数值稳定性修复，但尚未实现 B2 depth-validity gating。
4. `train-dev`/`val-dev` 开发划分、审计和使用边界已经完成；`val-dev` 固定为 318 条，split SHA-256 为 `1d0719d8f64f016d48995c25ab66d4004d76b7155d9efeef7cbb7454c0dd0e83`。
5. official test 继续保持 `sealed_unread`，未参与 B0 训练、checkpoint 选择或主评估。
6. 历史 A2 工具和 epoch-10、16 张 official-test pilot 只可作为 Depth 局部置零链路的历史依据，不能替代当前 RGB B0、完整 `val-dev`、预注册 mask/fill/seed 和 official-test 封存条件下的正式 A2。
7. 在本提示词形成时，A2/B2 计划曾拟从任务 1“A2 协议与本地准备”开始；该方向已于 2026-09-05 移入延期未执行区，当前不处于恢复点。正式 A2 尚未运行，B2 尚未实现。A2/B2 是独立的 Depth 机制分支，不是后验校准方向的强制前置条件。

### 拟新增但尚未实现的离线模块

1. 冻结 checkpoint 的 float32 logits 导出与一致性核验工具；
2. 独立 calibration 集协议及其冻结、审计记录；
3. 温度缩放、类别条件缩放、向量缩放、顶层置信度保序回归等离线校准器；
4. ECE、NLL、Brier Score、可靠性图、风险—覆盖曲线与 AURC 的离线评价工具；
5. 高置信错误、边界风险和区域风险的离线分析工具；
6. 在正式 A2 之外、专门服务于校准迁移研究的退化生成与审计工具。

这些模块均应位于冻结模型之外，不修改 DFormerv2-S 融合骨干，也不反向改变 epoch 420 checkpoint。

### 后续可选扩展

1. 严格 RGB-only 或 Depth-only 模型机制对照；
2. 退化质量条件化校准；
3. 跨矿井泛化或留一矿井实验；
4. 区域级拒识与整图复核排序；
5. 退化增强训练或其他非纯后验方法。

可选扩展需要另立配置、数据隔离和训练／评价协议，不作为主实验的前置条件，也不得写成当前已具备能力。

## 3. 与当前 MUSeg 边界的硬衔接

1. MUSeg 官方 `train.txt=1595`，官方 `test.txt=1576`。
2. 已冻结的开发划分继续作为数据职责依据；`val-dev` 已参与 checkpoint 选择与 B0 主评估，不能直接改名或复用为独立 calibration 集。
3. 当前 epoch 420 B0 已绑定 checkpoint、RGB/Depth 输入契约、split、FP32 主 evaluator、原始 Label 网格和逐类指标，足以作为校准方案设计、身份绑定和可行性评估的固定基础；实际代码与实验仍需另行确认范围。
4. 正式校准实验所需的**独立 calibration 集**必须先另立并冻结协议，明确其与 `train-dev`、`val-dev` 和 official test 的隔离关系。
5. official test 在开发期继续封存，不参与 calibration 集划分、校准器拟合、阈值、退化条件、方法选择或结果回调；任何未来解封都需要独立门禁和用户明确批准。
6. A2/B2 与后验校准是两条不同问题链，不存在必须先完成 A2/B2 才能设计方向1的研究依赖。方向1的实际代码实现、logits 导出、calibration 集构建、指标计算和实验运行仍须单独确认范围并取得相应授权；两条路线也不得相互借用未经预注册的数据或结论。
7. single seed 不能估计训练随机方差。若校准收益很小、接近训练波动，或准备支撑重要论文结论，应对 B0 和方法增加成对重复或额外 seed；当前不把三 seed 设为启动探索的前置条件。

---

# 第一部分：研究问题与假说

## 1. 项目事实与文献依据分离

### 项目事实

本项目将以 `DFormerv2-S + ham` 的 RGB-D 输出为唯一主研究对象。模型在 RGB 与 Depth 输入上产生 MUSeg 15 类逐像素 logits，后验校准只消费冻结 checkpoint 的输出，不参与融合特征学习。

### 文献依据

- MUSeg 为矿下 RGB-D 语义分割及 15 类像素级标注提供数据基础。[RE429]
- 既有矿下 RGB-D 研究通常侧重 PA、mPA、mIoU、边界或语义连贯性，可信度评价可作为补充维度。[RE165]
- 安全关键感知研究强调概率可靠性、高风险预测过滤与复核门控，但这些结果不能直接外推为本项目的安全保证。[RE068, RE131, RE226]
- 后验校准可在冻结模型外拟合概率映射或风险门控，这是本方案采用离线工具路线的文献动机。[RE136, RE226]
- 注意力、融合权重和模态可靠性建模不天然等于概率已经校准。[RE119, RE130, RE659]

文献结论只用于提出研究问题；本项目模型、配置、授权边界与完成状态以 `doc/main/MUSeg-current-status.md` 和当前冻结协议为准。

## 2. 核心矛盾

1. **分割准确率不等于概率可信。** 较高 mIoU 不能保证 0.9 的像素置信度确实对应约 90% 的正确率。
2. **概率校准不等于错误排序改善。** 单调校准可能降低 ECE，却不改变风险—覆盖排序，因此 AURC 可能保持不变。
3. **正常条件有效不等于退化条件有效。** 低照、模糊、Depth 空洞、遮挡、模态缺失、错位和复合退化可能导致校准迁移衰减。
4. **像素平均不等于空间风险可控。** 连续错误区域、边界错误或小目标错误可能被大量容易像素稀释。[RE165]

## 3. 核心科学问题

在冻结 `DFormerv2-S + ham` checkpoint 的前提下：

1. 后验校准能否改善正常条件下像素置信度与实际正确率的一致性？
2. 校准收益能否迁移到拟扩展的输入退化条件？
3. 校准后的风险分数是否能有效排序错误，从而改善风险—覆盖表现？
4. 像素级风险是否会掩盖连续错误区域，区域风险能否提供互补信息？
5. 若未来开展跨矿实验，源域冻结的校准器能否保持收益，其失效边界在哪里？

拒识在本文中只表示“像素、区域或图像需复核”，不等同于车辆控制安全、人身安全或真实传感器故障概率。

## 4. 可证伪假说

- **H1：概率校准有效性。** 至少一种后验方法能在独立评价数据上改善 ECE、NLL 或 Brier Score；其中全局温度缩放原则上保持逐像素 argmax，因此不应改变分割结果。
- **H2：阈值迁移有效性。** 在独立 calibration 集上冻结的风险阈值，能在未参与选择的评价数据上更接近预设风险约束，或在相同风险下保留更高覆盖率。
- **H3：校准与排序相互独立。** 保序校准可以改善概率数值，但不必改善 AURC；只有改变错误相对排序的风险分数才可能改善 AURC。
- **H4：退化迁移存在边界。** 正常条件拟合的校准器在单退化下可能仍有效，在复合退化或严重错位下可能衰减或反转。
- **H5：区域风险具有补充价值。** 区域聚合可能更容易发现空间连续错误和边界错误，但其收益必须以覆盖代价和误拒率共同验证。

---

# 第二部分：实验对象与对照边界

## 1. 主模型

主实验固定使用已经完成并验收的 `DFormerv2-S + ham` RGB-D Quick-B0：

- RGB 和 Depth 共同输入；
- 模型输出 15 类逐像素 logits；
- 训练采用随机尺度后裁剪到 `480×640`；正式评价使用 `msflip-whole-original-grid-v1`，所有 logits 恢复到原始 Label 网格后融合和计分；
- 最终 checkpoint 为 epoch 420 的 `selector-epoch-420.pth`，SHA-256 为 `f246a3afc50334c81302b7bfebdadf7cf37d00326bf1c3aa54f6a151754e3a1c`；
- 主结果为 mIoU `58.79`、mAcc `69.91`、mF1 `72.73`；checkpoint 选择和主评估均未读取 official test；
- 后验校准只消费冻结 logits，不为获得更好的校准结果重新训练、微调或从 epoch 420 继续训练融合骨干。

当前 single-seed B0 足以作为工具设计、方法选择和初步消融方案的固定基础，但不能估计训练随机方差，也不自动授权工具实现或实验。每种校准方法必须在同一 checkpoint、同一输入清单和同一份 float32 logits 上配对比较，不能以改变基础模型或重复推理口径制造优势。若收益很小、接近训练波动，或要形成重要论文结论，再为 B0 和对应方法预注册成对重复或额外 seed。[RE130]

## 2. 模态对照的正确解释

### 主研究对象

`DFormerv2-S` RGB-D 是唯一主研究对象。

### 缺失模态退化

- 将 RGB 输入置为全零，只表示 **RGB 缺失模态退化**；
- 将 Depth 输入置为全零，只表示 **Depth 缺失模态退化**。

这两种条件仍然调用已经按 RGB-D 训练的融合模型，因此不能称为严格的 RGB-only 或 Depth-only 模型。

### 可选的严格单模态模型

严格 RGB-only、Depth-only 机制对照需要：

1. 另立模型配置；
2. 明确输入与网络结构；
3. 另行冻结训练、模型选择和 checkpoint 协议；
4. 在相同数据隔离下独立训练和评价。

它们只作为后续可选机制对照，不是开展 RGB-D 后验校准主实验的前置条件。涉及单模态冲突分数的分析也必须等待严格单模态模型存在后再开展，不得用全零退化结果冒充单模态概率。原文涉及的通用分割架构文献仅保留为可选机制背景，不作为用其他骨干替换本项目 DFormerv2-S 的依据。[RE060]

## 3. 校准方法对照

主方案保留以下离线方法：

1. 未校准 Softmax；
2. 全局温度缩放；
3. 类别条件温度缩放；
4. 向量缩放；
5. 顶层置信度保序回归；
6. 可选的退化质量条件化校准。

全局温度缩放作为最低复杂度基线。类别条件缩放、向量缩放和条件化校准具有更高自由度，只有在独立 calibration 集规模与类别覆盖足以支持时才进入正式比较；其正则、回退规则和选择标准需在实现前另行冻结。[RE078, RE119, RE136]

保序回归仅映射“预测类别正确概率”，不虚构完整 15 类概率，因此不能与完整概率校准器直接比较多分类 NLL。[RE136, RE226]

## 4. 拒识与风险分数对照

拟比较：

1. 不拒识；
2. 未校准最大后验概率；
3. 校准后的预测类别正确概率；
4. 预测熵；
5. 区域聚合风险；
6. 仅用于分析上界的 oracle 错误排序。

oracle 只衡量可达到的排序上界，不作为部署方法。所有阈值或工作点只能在独立 calibration 数据上冻结，不能根据官方 test 或最终退化结果回调。[RE131, RE226, RE068]

---

# 第三部分：数据隔离与阶段前置条件

## 1. 已冻结的开发划分

当前开发数据边界已经完成并投入 B0 使用：

1. 官方 train 共 1595 张，按 location group 构建 `train-dev`/`val-dev`，同一 location group 不跨 split；
2. `val-dev` 固定为 318 条唯一记录，split SHA-256 为 `1d0719d8f64f016d48995c25ab66d4004d76b7155d9efeef7cbb7454c0dd0e83`；
3. `val-dev` 已用于训练期模型选择和冻结主 evaluator 的最终 checkpoint 裁决，因此不能直接改名为独立 calibration 集；
4. official test 共 1576 张，继续保持 `sealed_unread`；
5. 本方案不回写现有 split，也不根据后验校准结果重新切分开发数据。

## 2. calibration 集的后续扩展

后验校准需要与训练及模型选择数据职责分离的独立 calibration 集。该集合尚不存在，正式校准前必须另立并冻结协议，至少明确：

- 数据来源与允许用途；
- group-aware 防泄漏约束；
- 与 train-dev、val-dev、官方 test 的关系；
- checkpoint、校准器和阈值各自允许查看的数据；
- 列表、哈希、审计和用户门禁。

这里不预设具体比例，也不把 val-dev 直接改作 calibration。若未来协议需要从现有开发数据中扩展，必须先处理模型选择与校准职责冲突，再冻结新的清单；不能在得到结果后追溯调整。

数据隔离思想与独立条件可靠性评价可参考既有校准和门控研究。[RE136, RE131, RE226]

## 3. official test 与最终评价

official test 当前保持 `sealed_unread`，本方案不构成解封授权。只有在校准数据职责、方法、阈值、checkpoint 与评价协议全部预注册并冻结后，才能由用户通过独立门禁决定是否进行一次性 official-test 评价；single-seed B0 已完成或未来是否增加 seed，都不会自动触发解封。后验校准方案不得把 official test 变成 calibration 集、阈值选择集、退化参数选择集或看到结果后的方法回调集。

跨矿实验属于可选扩展。只有矿井身份、分组边界、源／目标域隔离和校准职责另行冻结后，才可使用“跨矿泛化”或“留一矿井”结论；当前不把该实验写成已经完成或主实验必需步骤。

---

# 第四部分：退化研究设计

## 1. 现有能力的准确边界

历史 A2 只提供 Depth 局部置零与敏感性 pilot 的链路依据。旧工具曾按掩码修改 Depth、加载 checkpoint 并执行 RGB-D 推理，但旧 epoch-10 checkpoint、16 张 official-test 样本和单一置零条件均不满足当前正式 A2 身份，不能作为当前 B0 的退化结论或 B2 开发门票。

在本文形成时，A2/B2 计划曾拟从任务 1“A2 协议与本地准备”开始；该方向已于 2026-09-05 移入延期未执行区，当前不处于恢复点。正式 A2 尚未运行，B2 尚未实现。该机制分支与后验校准方向相互独立，方向1的方案设计不以 `A2-pass` 为前提；实际离线 logits、校准工具、数据构建和实验仍需单独授权。

现有项目能力仍不能据此宣称已经实现：

- 完整退化生成套件；
- 完整校准 logits 导出；
- 多退化统一 manifest；
- 校准与风险评价；
- 当前 RGB B0 上的正式 A2 或 B2 结论。

历史 pilot 结果只保留为问题来源，不替代当前 epoch 420 checkpoint 上预注册、仅使用 `val-dev` 的正式实验。

## 2. 拟扩展退化条件

在当前 epoch 420 B0 身份冻结、且独立 calibration 协议完成后，可逐步扩展：

1. RGB 低照；
2. RGB 模糊；
3. Depth 局部空洞；
4. RGB 或 Depth 局部遮挡；
5. RGB 整模态缺失；
6. Depth 整模态缺失；
7. RGB-Depth 错位；
8. 复合退化。

退化类别的研究动机分别可参考质量感知、缺失模态、遮挡和弱标定研究。[RE752] [RE346, RE461] [RE438] [RE460]

本设计阶段不新增强度、核大小、遮挡比例、错位像素、组合清单等实现参数。正式实现前应基于文献全文、项目输入定义，以及当前 B0 或后续获批技术检查的证据另行预注册，且不得用最终评价表现倒推有利参数。未来正式 A2 最终冻结的 Depth corruption 矩阵只能在其适用范围内作为证据，不自动扩展为方向1的全部退化协议。

## 3. 退化实验控制原则

1. 正常与退化版本必须来自同一原始样本并共享标签；
2. 退化生成记录样本身份、条件与配置，保证可审计；
3. 校准器、风险阈值和退化条件的选择互相隔离；
4. 全零 RGB／Depth 只标记为缺失模态退化；
5. 合成退化结果不外推为真实粉尘浓度、设备寿命或物理故障概率；
6. 已知退化和未见复合退化的用途需在实现前冻结。

质量自适应和模态可靠性研究可作为后续条件化校准的动机，但不证明当前模型已有对应能力。[RE955, RE752, RE119]

---

# 第五部分：冻结 logits 与后验校准

## 1. logits 采集原则

后验校准必须建立在冻结 checkpoint 与**同一批 float32 logits**上：

1. 每个正式 checkpoint 固定模型权重、推理配置和输入清单；
2. 对每个样本保存或可审计地重建 15 类逐像素 float32 logits；
3. 各校准方法读取同一份 logits、标签和有效像素掩码；
4. 校准比较不得因重复推理、混合精度差异或样本过滤差异产生输入不一致；
5. `ignore index` 像素不参与分割、校准或拒识指标；
6. logits 产物记录 checkpoint、split、配置和生成命令的可追溯标识；
7. 校准器只在独立 calibration 集拟合，随后冻结并应用到预定评价条件。

概率输出和不确定性评价的研究动机来自安全关键感知与校准工作。[RE131, RE136, RE666]

## 2. 全局温度缩放

设冻结 logits 为 $z_{i,c}$，温度为 $T>0$：

$$
p_{i,c}(T)=\frac{\exp(z_{i,c}/T)}{\sum_{k=1}^{15}\exp(z_{i,k}/T)}.
$$

温度仅在独立 calibration 集上通过预先冻结的目标拟合。拟合后不针对官方 test、具体矿井或某个退化条件重新调节。由于正温度对同一像素各类别 logits 做统一缩放，argmax 应保持不变；实现时需要核验预测标签和 mIoU 的一致性。[RE136, RE226]

本阶段不冻结优化器、迭代次数、温度范围或采样规模，这些属于离线工具实现前的协议事项。

## 3. 类别条件温度与向量缩放

类别条件温度和向量缩放用于检验类别差异与更高自由度映射：

$$
z'_{i,c}=a_c z_{i,c}+b_c.
$$

正式开展前必须明确训练—推理定义、参数约束、正则、类别覆盖不足时的处理和模型选择规则。若方法改变 argmax，必须同时报告改变像素比例、每类 IoU 与 mIoU，不能把它描述成“分割完全不变”的纯温度校准。[RE078, RE119, RE136]

## 4. 顶层置信度保序回归

以最大后验概率 $s_i$ 和正确性标签 $r_i=\mathbb{1}(\hat y_i=y_i)$ 拟合单调映射：

$$
f(s_i)\approx P(r_i=1\mid s_i).
$$

该方法只产生预测类别的正确概率，可用于顶层 ECE、可靠性图和选择性拒识。因为映射保序，风险—覆盖排序与 AURC 通常不变；若 ECE 改善但 AURC 不变，应解释为“概率值改善、排序未改善”，而不是失败。[RE136, RE226]

## 5. 正常条件与混合条件校准

优先研究只用正常条件独立 calibration 数据拟合的校准器，以检验其向退化条件迁移的能力。混合退化校准器、按质量条件化校准器或按退化类型分别拟合的校准器均属于可选扩展；其数据组成与使用条件必须另行冻结。若推理时需要真实退化标签选择校准器，应标记为 oracle 条件化分析，而不是直接部署方案。[RE955, RE752, RE119]

---

# 第六部分：评价体系

## 1. 基础分割性能

对冻结模型及可能改变 argmax 的校准方法报告：

- PA、mPA；
- 每类 IoU 与 mIoU；
- 必要时的边界区域 IoU。

全局温度缩放和顶层保序映射若不改变类别预测，其分割指标应保持一致。文献中的矿下语义分割指标仅作为评价动机，不替代本项目正式实验结果。[RE429, RE165]

## 2. 概率校准

保留以下研究指标：

- ECE；
- 自适应分箱 ECE；
- 类别条件 ECE；
- NLL；
- 多分类 Brier Score；
- 可靠性图。

NLL 与多分类 Brier Score 只用于具有完整 15 类概率输出的方法。分箱、聚合单位、类别不足处理与置信区间方案在工具实现前另行冻结，不在当前设计中写死。[RE666, RE131, RE226]

Brier Score 可写为：

$$
\mathrm{Brier}=\frac{1}{N}\sum_i\sum_c\left(p_{i,c}-\mathbb{1}(y_i=c)\right)^2.
$$

## 3. 风险—覆盖与 AURC

对风险分数 $q_i$，定义接受集合：

$$
A(\tau)=\{i:q_i\ge\tau\}.
$$

覆盖率与选择性风险分别为：

$$
C(\tau)=\frac{|A(\tau)|}{N_{\mathrm{valid}}},
\qquad
R(\tau)=\frac{\sum_{i\in A(\tau)}\mathbb{1}(\hat y_i\ne y_i)}{|A(\tau)|}.
$$

正式报告应包含风险—覆盖曲线、AURC、固定覆盖率风险，以及 calibration 集冻结阈值在独立评价条件下的实际风险和覆盖率。具体覆盖率点、目标风险和阈值选择规则尚未冻结，留待离线工具协议确定。[RE131, RE226, RE068]

## 4. 高置信错误

保留两种互补口径：

$$
\mathrm{HCE}_{all}(t)=
\frac{\#\{i:\hat y_i\ne y_i,q_i\ge t\}}{N_{\mathrm{valid}}},
$$

$$
\mathrm{HCE}_{conf}(t)=
\frac{\#\{i:\hat y_i\ne y_i,q_i\ge t\}}{\#\{i:q_i\ge t\}}.
$$

阈值必须在正式分析前冻结，不能依据最终结果选择。结果可按类别、矿井、退化和边界／内部像素分层，但跨矿分层仅在对应协议完成后开展。[RE068, RE226]

## 5. 统计单位、single seed 与后续重复

1. 当前主研究对象是已冻结的 single-seed Quick-B0；它足以作为工具设计、可行性评估、方法淘汰和初步消融方案的固定基础，但不自动授权实现或实验；
2. 当前结果不得写成三 seed 均值、随机方差或统计显著性结论；
3. 所有校准方法在同一 checkpoint、同一输入清单和同一份 logits 上使用配对分析；
4. 重采样和置信区间以图像或 location group 为单位，避免把像素错误视为完全独立；
5. 若收益很小、接近训练波动或要支撑重要结论，必须在看到最终结论前预注册 B0/方法的成对重复或额外 seed，并让每个 seed 的校准器基于该 seed 的冻结 logits 独立拟合；
6. 具体 bootstrap 次数、显著性检验和多重比较规则在实现前冻结。

多随机种子与分层不确定区间的研究动机参考相关 benchmark 和稳定性工作。[RE130, RE447]

---

# 第七部分：区域风险与机理分析

## 1. 区域风险

区域风险用于检验像素平均指标是否掩盖连续错误块，作为像素级结果的补充，而非替代：

1. 区分标签边界与内部像素；
2. 从预测结果中提取同类连通区域；
3. 聚合区域内像素风险；
4. 定义可审计的区域正确性或区域匹配规则；
5. 报告区域覆盖率、错误区域拒识率、正确区域误拒率和区域 AURC；
6. 若区域定义对参数高度敏感，只报告为探索性分析。

边界宽度、连通性、最小区域面积、匹配阈值和聚合函数尚未冻结，需在离线模块设计时预注册，不在当前方案中填入未经项目验证的数值。区域与边界研究动机来自矿下语义连贯性和不确定性过滤工作。[RE165, RE068, RE131]

## 2. 质量—冲突—失校准机制

可在主校准结论之后探索：

- RGB 质量与失校准的关系；
- Depth 有效率与高置信错误的关系；
- RGB-Depth 对齐质量与校准迁移的关系；
- 若未来严格单模态模型已训练，再研究跨模态预测冲突。

质量感知、特征匹配、异质性与不确定性加权可作为方法线索。[RE752, RE955, RE415, RE078, RE119, RE659]

具体质量公式、边缘容差、分层数量和统计模型都属于后续可选扩展。当前不把它们写成已实现，也不以全零输入退化替代严格单模态概率来计算冲突。

---

# 第八部分：结果判定与证伪规则

## 1. “校准有效”

只有在未参与拟合与选择的独立评价数据上，ECE、NLL 或 Brier Score 等预注册指标得到稳定改善，才支持概率校准有效。calibration 集自身改善不能作为独立证据，可靠性图用于辅助检查指标是否被样本分布掩盖。

## 2. “不损害分割精度”

- 全局温度缩放应保持 argmax 与分割指标；
- 向量缩放等可能改变 argmax 的方法必须报告分割变化；
- 不预先填写尚未冻结的精度容忍阈值。

## 3. “选择性拒识有效”

ECE 改善不自动证明 AURC 改善。只有风险—覆盖结果或冻结阈值在独立评价数据上表现更好，才能支持拒识有效；若风险约束频繁被违反，应报告阈值迁移失败。

## 4. “退化鲁棒”

正常、单退化和复合退化应分别报告，不能只给所有条件平均值。退化强度与组合必须事前冻结；合成退化只能支持对应合成条件的结论。

## 5. “跨矿泛化”

跨矿实验尚未开展。只有完成矿井身份核验、group-aware 隔离和独立 calibration 协议后，才能报告各目标矿井、宏平均与最差矿井结果。

## 6. 停止或降级条件

出现以下情况时停止对应高自由度实验或降级为探索性分析：

1. epoch 420 B0 的 checkpoint、split、输入契约、FP32 evaluator 或原始 Label 网格身份无法闭合；
2. 独立 calibration 集尚未冻结；
3. logits 无法证明来自同一冻结 checkpoint、同一输入清单和 float32 输出；
4. calibration 数据类别覆盖不足以支撑类别条件方法；
5. 区域结论对未冻结参数高度敏感；
6. 校准在未见退化下失效。

第 6 项本身可以成为“校准迁移边界”的研究结论，不应通过回调最终评价阈值掩盖。

---

# 第九部分：预期贡献与叙事结构

## 1. 预期贡献

若实验支持假说，本研究可形成：

1. 面向本项目 `DFormerv2-S + ham` 的训练、模型选择、独立校准与评价隔离方案；
2. 对概率校准、错误排序和阈值迁移三类结论的明确区分；
3. 对正常、缺失模态和拟扩展退化条件下可信度边界的系统评价；
4. 像素风险与区域风险的互补分析。

本研究不主张提出新的校准理论，也不把 DFormerv2-S baseline 表述为 MUSeg 论文 DFormer-S 的严格数值复现。

## 2. 论文叙事建议

1. 以已验收的 single-seed RGB Quick-B0 作为固定起点；
2. 再证明未校准置信度在正常或退化条件下是否存在问题；
3. 区分概率值、错误排序与阈值迁移；
4. 比较温度缩放、向量缩放和保序回归；
5. 报告风险—覆盖与区域风险；
6. 最后讨论退化和可选跨矿扩展中的成功与失败边界。

潜在结论应保持可证伪表述：简单后验校准可能改善同域概率可信度，但不自动改善错误排序，其风险阈值在跨退化或跨矿分布偏移下可能存在迁移边界。

---

# 与当前 MUSeg 阶段的衔接和实施顺序

## A. 已完成且可直接作为方向1起点的部分

1. 已完成 single-seed RGB Quick-B0 的 500 epochs 训练和 4 个候选主评估；
2. 最终基础模型固定为 epoch 420，主 mIoU `58.79`、mAcc `69.91`、mF1 `72.73`；
3. checkpoint、RGB/Depth 输入契约、318 条 `val-dev`、split SHA、FP32 主 evaluator、原始 Label 网格和 official-test 未参与状态均已绑定；
4. `DFormerv2-S + ham` 已具备 MUSeg 15 类逐像素 logits 推理链路；
5. 现有 single-seed B0 已足够作为离线工具设计与初步方法方案的固定起点，不再把三 seed baseline 设为设计前置条件；工具实现、数据处理和实验仍需另行授权。

## B. 启动正式校准实验前必须完成的前置工作

1. 另立并冻结独立 calibration 集协议，明确与 `train-dev`、已参与 checkpoint 选择的 `val-dev`、以及 official test 的职责隔离；
2. 冻结 epoch 420 checkpoint、输入清单、FP32 推理配置、ignore mask、logits 格式与哈希身份；
3. 冻结校准器拟合目标、参数约束、方法选择规则、风险阈值和评价指标；
4. 明确哪些工作只属于工具开发／探索，哪些结果要支撑重要结论并因此需要成对重复或额外 seed；
5. 继续保持 official test `sealed_unread`，任何解封都另经独立门禁和用户明确批准；
6. 单独取得代码实现、GPU、长耗时评估、训练或云资源所需授权。本文不自动授权这些操作。

A2/B2 深度有效性阶段不在上述研究依赖链中。在本提示词形成时，该方向曾拟从 A2 协议与本地准备开始；它已于 2026-09-05 移入延期未执行区，正式 A2 尚未运行、B2 尚未实现。其结果可以在未来作为 Depth 退化机制证据。方向1的方案设计无需等待 A2，但独立 calibration 集构建、logits 工具实现和任何实验仍需重新确认范围并取得授权。

## C. 拟新增离线校准阶段

1. 建立 float32 logits 导出、哈希与一致性核验；
2. 在同一批 logits 上比较未校准 Softmax 与全局温度缩放；
3. 根据 calibration 数据的类别覆盖和低自由度结果，再决定是否加入类别条件温度、向量缩放与保序回归；
4. 建立 ECE、NLL、Brier、可靠性图、风险—覆盖、AURC、高置信错误和区域风险工具；
5. 分别冻结并回答“概率数值是否改善”“错误排序是否改善”“阈值是否迁移”三个问题；
6. 在不修改 epoch 420 融合骨干和 checkpoint 的前提下运行正式离线比较。

## D. 可选扩展

主校准结果稳定后，再决定是否开展：

1. 低照、模糊、Depth 空洞、遮挡、单模态缺失、错位与复合退化；
2. 将未来正式 A2 的预注册 Depth corruption 作为一个独立、身份明确的退化证据来源；
3. 严格 RGB-only／Depth-only 模型训练与机制对照；
4. 条件化校准；
5. 跨矿实验；
6. 更完整的区域或整图复核研究。

以上扩展均需独立门禁或协议，不改变当前 epoch 420 B0、既有开发 split 或 official-test 封存状态，不作为主实验启动条件，也不得在实施前写成已完成。
