
---
name: research-progress-report
description: 根据当前对话中指定时间段内的工作内容，生成科研、开发或项目组会汇报。适用于阶段总结、工作汇报、实验汇报、代码改动说明和项目交接。论文、数据集、实验与代码分析均为可选内容。
---
# 工作汇报生成 Skill

## 目标

将当前对话中一段时间内完成的工作整理为清晰、准确、适合汇报的报告。

本 Skill 关注“这段时间做了什么”，不默认重新分析整个项目。

默认时间范围为：

> 从当前对话中的上一次正式工作报告之后，到用户本次提出汇报请求之前。

如果找不到上一次报告、时间边界不明确，或者对话中包含多个项目，应先向用户确认汇报范围。

提示词、Skill 定义和格式讨论不自动视为“正式工作报告”。

## 汇报前确认

如果用户没有明确说明汇报内容，先用一个简短问题确认：

> 本次是否汇报从上一次正式报告之后到现在的全部进展？
> 可以重点包含：代码改动、实验结果、数据处理、论文调研、问题分析、方案决策、当前困难和下一步计划。请告诉我需要包含哪些内容，以及汇报对象或详细程度。

用户可以回答“全部”“只汇报代码和实验”或直接指定时间和主题。

如果用户已经明确范围和内容，不要重复询问，直接生成报告。

## 基本原则

1. 以指定对话区间中的实际工作为主，不擅自回顾区间外内容。
2. 论文和数据集分析是可选项，只有用户要求或它们确实参与了本阶段工作时才写入。
3. 代码文件名、函数名、参数、命令和实验结果必须来自已有证据。
4. 区分以下状态：
   - 已完成并验证；
   - 已完成但尚未验证；
   - 正在进行；
   - 仅提出或计划实施。
5. 不得把讨论过的方案写成已经完成的工作。
6. 不得虚构实验结果、代码改动、论文结论或数据统计。
7. 信息不足时使用“尚未验证”“当前对话未提供”或“需要补充”，不要自行猜测。

## 执行流程

### 1. 确定汇报边界

识别汇报的起点、终点、项目和用户指定的重点。

### 2. 提取阶段工作

从指定对话区间中提取：

- 本阶段目标；
- 遇到的问题；
- 已采取的措施；
- 代码、配置或数据改动；
- 实验和验证结果；
- 方案选择及原因；
- 尚未解决的问题；
- 下一步计划。

### 3. 按需检查补充证据

只检查与本次汇报有关的材料：

- 涉及代码改动时，可检查相关文件和 Git diff；
- 涉及实验时，可检查日志、配置和结果；
- 涉及数据处理时，可检查数据格式、处理流程和统计结果；
- 涉及论文时，可对照论文、摘要或官方实现；
- 用户只要求整理对话时，不必额外展开项目调查。

### 4. 组织汇报逻辑

每项主要工作尽量说明：

> 问题是什么 -> 为什么要处理 -> 做了什么 -> 原理是什么 -> 当前结果如何 -> 后续还需要什么

### 5. 补充可复现信息

仅在存在代码改动或用户要求交接、复现时，补充：

- 文件路径和类或函数名；
- 改动前后的行为；
- 关键参数和实现原理；
- 环境、数据和配置要求；
- 执行命令；
- 验证方法和预期结果。

无法确认的信息标记为 `<待补充>`。

## 输出方式

根据本次内容选择章节，不要求每次输出所有部分。默认可使用：

# 阶段工作汇报

## 一、本阶段工作概述

概括汇报周期、目标和当前状态。

## 二、主要工作进展

按工作项说明问题、处理方式、原理和结果。

## 三、代码与配置改动

仅在存在相关改动时输出。列出文件名、位置、改动内容和作用。

## 四、实验与验证结果

仅在存在实验或验证时输出。区分已验证结果与尚未验证结论。

## 五、数据集或文献分析

仅在本阶段实际涉及数据集或参考论文时输出。

## 六、当前问题

说明未解决问题、风险和受阻原因。

## 七、下一步计划

给出具体且可以执行的后续工作。

## 八、复现或交接说明

仅在用户需要复现、迁移或项目交接时输出。

报告应适合直接用于组会汇报，重点突出实际进展、依据、结果和待办，避免写成完整论文综述或整个项目说明书。
