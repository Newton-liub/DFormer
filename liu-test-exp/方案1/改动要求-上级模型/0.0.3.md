## 0. 本轮授权

现在正式授权：

```text
MMFR-A2-v3-exploratory-one-arm-screening
```

执行：

```text
MMFR-A2-depth-corruption-train-v3
500 epochs
RTX 4090
batch size = 10
```

本轮**只训练 corruption 一侧**。

仍然不授权：

```text
MMFR-A2-clean-control-v3 500e
RTX 5090 probe
B1
R1/S1/C1
official test
修改科学协议
```

第一阶段结束以后必须停止并等待高级审查。

---

## 1. 冻结当前生产身份，不再继续优化

正式运行必须使用当前已经通过全部验证的实现：

```text
HEAD:
34278b5

tag:
MMFR-A2-v3-pipeline-opt1-freeze

scientific parent:
MMFR-A2-train-integration-v3

scientific_semantics_changed:
false
```

正式 production helper 必须对应当前冻结版本：

```text
SHA-256:
9ab1c852...
```

禁止在训练前继续做：

```text
ThreadPool
ProcessPool
CPU/GPU overlap
torch.compile
CUDA corruption
DataLoader workers 调整
```

正式：

```text
DataLoader workers = 8
```

保持不变。

Opt-B 的实验结论记录为：

```text
Opt-B parallelization rejected
```

不要把 Opt-B 实验代码偷偷带入 production path。

---

# 2. 运行前做一次最终 preflight

在启动 500e 之前只做一次短 preflight，不要重新跑大规模 qualification。

必须检查：

### source

```text
git status --porcelain = empty
HEAD = 34278b5
tag resolves correctly
```

### pretrained

确认：

```text
DFormerv2_Small_pretrained.pth
SHA-256 =
19116988fc86dc9f3e879282237941e11b9b1b5c480edb51e92807311dbc11a6
```

### data

确认：

```text
train-dev = 1277
val-dev = 318
official test = sealed_unread
```

已有 dataset manifest/hash 不得变化。

### training

确认：

```text
seed = 772961337
batch_size = 10
workers = 8
epochs = 500
AdamW
lr = 6e-5
warmup = 10 epochs
poly power = 0.9
AMP = true
SyncBN = true
torch.compile = false
lambda_rel = 0.1
```

确认：

```text
A + MID-A
Depth-only corruption
Depth-only reliability supervision
```

全部与冻结 protocol 一致。

任何一项不同：

```text
STOP
```

不得自动修改后继续。

---

# 3. 正式 run identity

新建执行身份，例如：

```text
run_id =
mmfr-a2-v3-one-arm-screening-seed772961337-4090-opt1
```

明确记录：

```text
phase = exploratory-one-arm-screening
scientific_protocol = MMFR-A2-train-integration-v3
implementation = MMFR-A2-v3-pipeline-opt1-freeze
```

不得写：

```text
paper-confirmation
formal-paired-experiment
official-test
```

因为现在只有一支模型。

---

# 4. 不允许从任何旧 checkpoint 续训

训练必须从：

```text
official pretrained
```

独立开始。

禁止：

```text
Quick-B0 checkpoint
v1 checkpoint
v2 checkpoint
旧 probe checkpoint
checkpoint-gate checkpoint
```

作为初始化。

只有本次 500e 自己在运行过程中发生中断时，才允许从**本 run 自己的 latest checkpoint** resume。

---

# 5. checkpoint 和恢复策略保持冻结

不要重新设计 checkpoint policy。

继续使用当前 v3 已冻结并通过 save/reload 门禁的：

```text
selector
checkpoint retention
latest checkpoint
best checkpoint
candidate manifest
```

训练发生正常 checkpoint 时，至少记录：

```text
epoch
global_optimizer_step
checkpoint SHA-256
best_val_mIoU
best_epoch
optimizer state
AMP scaler state
RNG state
```

如果进程意外中断：

1. 不从头自动重跑；
2. 校验 latest checkpoint SHA-256；
3. 使用既有 strict resume；
4. 记录 interruption；
5. 继续完成同一 run identity。

不得把“中断后重新从官方 pretrained 开始”伪装成同一训练。

---

# 6. 云端生命周期保护

预计：

```text
training + validation:
约 18.6–20.8 小时
```

按当前：

```text
1.88 元/小时
```

预计：

```text
约 35–39 元
```

为了防止异常空转，正式启动前请确认用户侧已经设置合理的**最晚停止保护**。

建议最大运行窗口约：

```text
26–28 小时
```

不是要求训练 28 小时，而是作为异常情况下的计费保险。

如果下级模型无法操作平台控制面，则必须在开训前报告：

```text
ACTION REQUIRED FROM USER:
confirm maximum stop schedule / billing protection
```

用户确认以后再启动。

---

# 7. SwanLab 不作为阻塞条件

如果 online 凭据现在仍不可用：

```text
继续 offline
```

不要为了 SwanLab API key 阻塞科学实验。

但必须保证本地持续保存：

```text
epoch
iteration
seg loss
reliability loss
total loss
learning rate
AMP scale
throughput
GPU memory
telemetry categories
validation metrics
```

SwanLab online 只是方便监控，不是训练正确性的门禁。

---

# 8. 正式训练中不要保留重型 probe 哈希

之前每 step：

```text
tensor -> CPU -> SHA256
```

属于 probe instrumentation，不属于正式生产训练。

正式 500e：

**关闭这种每 step 的重型审计哈希。**

否则会浪费已经优化出来的吞吐。

但保留轻量运行 telemetry：

```text
step time
loss
AMP
VRAM
epoch
iteration
```

以及周期性 checkpoint/hash。

正式训练预期吞吐参考使用：

```text
约 9.9 img/s
```

而不是 audit probe 的 7.81 img/s。

---

# 9. 训练前 30 分钟设置运行健康监测

前期重点观察：

```text
loss finite
AMP GradScaler 正常
无 OOM
throughput 大致稳定
GPU memory 不持续泄漏
DataLoader 无异常卡死
corruption telemetry 正常
```

可以报告：

```text
rolling 100-step img/s
```

如果长期明显低于优化后 probe，例如跌到接近：

```text
< 7 img/s
```

且不是 validation/checkpoint 阶段，应先诊断，而不是默默跑 20 小时。

但：

**禁止因为 mIoU 看起来低而提前停止。**

这次仍是冻结的 500 epoch 训练。

性能结果不能触发改变 epoch 数。

---

# 10. 工程停止条件

以下任一发生，立即暂停训练：

```text
NaN/Inf loss
持续 OOM
checkpoint 无法保存
resume state 不完整
official test 被意外访问
source/worktree 在运行中变化
training config 与 frozen identity 不一致
validity contract assertion failure
corruption target assertion failure
数据损坏
长时间吞吐异常且无法解释
```

允许修复的只能是纯运行环境问题。

若需要修改科学代码：

```text
STOP
```

不得使用当前 run identity 继续。

---

# 11. 不允许结果驱动修改

500 epoch 期间禁止：

```text
看前 100 epoch 效果差就提高 corruption 概率
修改 lambda_rel
改变 severity
减少 blur/misalignment
调整 p_clean
修改 LR
修改 batch size
换 5090
改变 selector
```

即使觉得“这样可能更好”，也不得在当前 run 中改。

先把冻结实验完整跑完。

---

# 12. 训练期 telemetry 要保留四类

继续记录：

```text
natural-invalid
synthetic-invalid
implicit-quality
valid-clean
```

至少每 epoch 汇总：

```text
pixel count
pixel proportion
Depth BCE mean
```

但 telemetry：

```text
只观察
不参与 reweight
```

不得根据实时 telemetry 调 loss。

---

# 13. selector 仍然只看 clean val-dev

Checkpoint selector 保持：

```text
clean val-dev
original-full
scale = 1.0
no flip
metric = mIoU
tie-break = earlier_epoch
```

failure conditions：

```text
不得参与 checkpoint selection
```

训练结束以后才冻结最终 selected checkpoint。

---

# 14. 500 epoch 完成后先冻结 checkpoint

训练结束以后：

1. 完成既有 selector；
2. 确定 selected epoch；
3. 保存最终 selected checkpoint；
4. 计算 SHA-256；
5. 保存 candidate manifest；
6. 保存训练 summary；
7. 保存代码 commit/tag；
8. 保存完整成本/耗时。

然后：

```text
禁止继续训练
```

---

# 15. 随后执行 one-arm screening evaluator

只在 selected checkpoint 冻结以后进行。

必须使用当前已经冻结的：

```text
msflip-whole-original-grid-v1
```

以及冻结：

```text
evaluation seed = 2026091401
```

至少评价：

### clean

```text
clean
```

### 六个 single failure

```text
spatial_dropout@0.75
gaussian_noise@0.75
blur@0.75
quantization@0.75
misalignment@0.75
entire_missing@1.0
```

### 三个 mixed failure

```text
spatial_dropout@0.5 + gaussian_noise@0.5
blur@0.5 + misalignment@0.5
quantization@0.5 + misalignment@0.5
```

不得为了 screening 重新设计条件。

---

# 16. 同时评价 Quick-B0 历史参考

使用现有 Quick-B0 checkpoint。

在**相同 evaluator 和相同 failure realization**上生成参考结果。

所有表格中必须称：

```text
Quick-B0
historical RGB development reference
```

禁止称：

```text
clean control
```

Quick-B0 只是判断：

> 是否值得再花第二份 500e 的钱。

---

# 17. screening 报告必须包含

生成例如：

```text
one-arm-screening-report.json
one-arm-screening-report.md
```

至少包含：

### Training

```text
selected epoch
best clean val mIoU
500e total wall time
GPU hours
estimated RMB
mean production throughput
checkpoint SHA-256
commit/tag
```

### Clean

```text
mIoU
mAcc
mF1
Boundary IoU
```

### 六种 single failure

每项：

```text
mIoU
mAcc
mF1
Boundary IoU
```

以及：

```text
six-single-failure macro mIoU
```

### 三种 mixed

同样输出。

### Reliability telemetry

```text
natural-invalid
synthetic-invalid
implicit-quality
valid-clean
```

训练期整体趋势。

### Historical comparison

逐条件：

```text
corruption-v3
Quick-B0
raw difference
```

但标题必须写：

```text
exploratory comparison against historical RGB reference
```

---

# 18. 本阶段只允许三个结论

训练+评价以后只能给：

```text
screening-promising
screening-unpromising
screening-inconclusive
```

禁止：

```text
MMFR proven effective
A2 supported
causal robustness improvement
SOTA
正式论文结果
```

因为还没有：

```text
MMFR-A2-clean-control-v3
```

这个真正公平的 twin control。

---

# 19. 不要由下级模型自己造 screening 阈值

不要新增：

```text
“超过 Quick-B0 0.5 就 promising”
“至少四类上涨”
```

之类的门槛。

只输出数据。

高级模型根据：

- clean performance；
- 六种 corruption；
- macro robustness；
- failure response pattern；
- reliability telemetry；
- 训练成本；

决定值不值得补跑 clean-control。

---

# 20. screening 完成以后必须停

状态改为：

```text
one-arm-screening-completed-awaiting-senior-review
```

明确：

```text
clean-control-v3 500e:
NOT AUTHORIZED

B1:
NOT AUTHORIZED

5090:
NOT AUTHORIZED

official test:
SEALED / UNREAD
```

不要因为结果看起来很好就自动启动 clean control。

---

# 21. 最终回来只给我这 8 项

不要一开始发几百行日志。

只汇报：

**① 正式训练运行事实**

```text
500 epochs 是否完整
selected epoch
wall time
GPU hours
费用
mean throughput
是否发生 resume
```

**② selected clean 指标**

```text
mIoU
mAcc
mF1
Boundary IoU
```

**③ 六种 single failure**

一个简洁表。

**④ 六单 failure macro-average**

**⑤ 三种 mixed failure**

一个简洁表。

**⑥ Quick-B0 同条件历史参考**

逐条件差值。

**⑦ reliability telemetry**

重点说明：

```text
implicit-quality
```

是否确实学到非 trivial 信息。

**⑧ 异常和边界**

包括：

```text
AMP skips
OOM
resume
VRAM
unexpected throughput
任何 assertion
official_test_included=false
```

最后只写：

```text
screening-promising /
screening-unpromising /
screening-inconclusive
```

不要自行申请下一实验。

---

## 对当前性能优化阶段的正式裁决

把下面这句话也写入状态：

> **MMFR-A2-v3 pipeline optimization accepted. Opt-A is adopted; Opt-B thread parallelization is rejected; implementation parity and GPU training-trajectory equivalence are PASS. RTX 4090 optimized production-like throughput is sufficient for the one-arm 500-epoch screening. No RTX 5090 probe is required before this screening.**

现在最重要的已经不是再把 **9.90 img/s** 优化成 11 或 12。

现在最重要的是花大约 **35–39 元**回答真正的科学问题：

> **训练 500 epoch 以后，它到底有没有鲁棒性提升的苗头？**

这个答案比继续折腾流水线更值钱。