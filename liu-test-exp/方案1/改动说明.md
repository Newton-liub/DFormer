我已经把这次上传的 **v2→v3《改动细节2》** 和 **v3《独立审计总规划》** 对照上一轮高级审计要求重新核了一遍。

**正式结论：v3 可以维持** `eligible-for-cloud-capacity-probe-authorization`**。不需要再补文献，也不需要再做第四轮科学语义修改后才能进入 cloud probe。** 你要求的 A + MID-A 已经从“决策”真正落到了 corruption kernel、sequential validity state、target、blur、misalignment、训练守卫和资格工具中，而且 v2 没有被原地覆盖。

更具体地说，当前版本已经满足我上一轮给出的关键条件：intensity corruption 不复活 invalid Depth；blur 改成 mask-normalized；misalignment 同时搬运 Depth 和 validity；最终监督改为 `V_state_final × R_syn`；并硬约束最终 state、`depth_valid_post` 和实际非零 Depth 一致。 五项 v3 本地资格也分别覆盖了 severity/burden、CPU contract、validity transport、初始状态公平性和真实 AMP/GradScaler 更新路径，而且全部 PASS。

### 当前我只剩两个需要你注意的点

第一，**不需要因此继续造 v4**。现在继续修改 corruption taxonomy、loss weighting、43 个 optimizer-missing 参数、8 个无有效 Depth crop，反而会重新打开已经关闭的科学变量。当前总规划正确地把这些项冻结住了：六类 failure、`p_clean=0.25`、`lambda_rel=0.1`、500 epoch、optimizer、selector 和主成功门槛都没有因为 v3 而漂移。 43 个参数当前保持 inherited optimizer semantics 也是合理选择；现在修它会把“optimizer 修复效果”和“MMFR corruption robustness”混在一起。

第二，**cloud probe 之后还不能直接跳到 500 epoch**。你文档里已经保留了“checkpoint save/load 尚未验收”这一风险，我认为应该继续把它作为正式长训练前的最后一个工程门禁，而不是再搞新的科学门禁。 建议下一阶段严格按下面顺序走：

1. 用户单独授权云端 **batch size 10 capacity/throughput probe**。4090 优先；probe 只回答显存、AMP finite、吞吐和单位样本成本，不读 official test、不跑完整 evaluator。当前文档对此授权边界写得正确。
2. probe 中最好让 **clean-v3 和 corruption-v3 两个身份都至少真实跑若干 step**，确认 4090 环境没有环境特异问题；不要仅测试 corruption 一侧。
3. 在正式 500 epoch 前做一次很小的 **checkpoint save → destroy process → strict reload → optimizer/GradScaler 恢复 → continue-step** 验收。要求模型参数、optimizer state、GradScaler、epoch/iteration、scheduler 和 RNG/训练身份恢复正确。它是工程恢复能力验收，不是新的科学实验。
4. 同一次云任务必须验证控制面停止流程，最终确认实例状态为 `Stopped`。
5. 这些通过后，再单独授权两个公平身份的正式 500 epoch。probe 的授权不能自动升级成训练授权——你当前恢复点已经正确写成这个逻辑。



### 一个很小的文档语义提醒，不阻塞 probe

现在四类 telemetry 中：

- `natural-invalid` = 原始坐标 `V_pre=0`
- `synthetic-invalid`
- `implicit-quality`
- `valid-clean`

在 **MID-A misalignment** 下，一个原本 `V_pre=0` 的目标坐标有可能因为别处的合法 Depth/validity 一起平移进来而变成 `V_post=1`。因此未来解释 `natural-invalid BCE` 时，不要把它写成“这一类 target 必然为 0”。它真正表示的是：

> **该目标坐标在 corruption 前原始 Depth 无效。**

而不是：

> **最终输入在该坐标没有 Depth。**

你当前代码把 telemetry 只用于 detached 统计、不参与 loss weighting，所以这**不是实现错误，也不值得为了它建立 v4**。 但以后做 R1 时，`explicit-invalid` 最好按 **最终** `V_post=0` 定义，而不要直接等同于训练 telemetry 的 `natural-invalid`。这样 MID-A 后的统计解释会更干净。

除此之外，我没有发现新的、足以阻止 cloud capacity probe 的科学问题。

所以这一次我的审计结论可以明确收口为：

> `通过当前高级审计；MMFR-A2-train-integration-v3 可申请云端 capacity/throughput probe 授权。`

同时仍然严格保持：**没有 MMFR 性能结论、没有正式 checkpoint、没有正式评价、没有 official-test 结果，也没有 500 epoch 训练授权。** 你的总规划目前对这一边界写得是正确的。





# 最新要求

1. **将当前 A2 正式实验流程改成“两阶段省钱策略”，但不得修改任何 v3 科学变量。** 保持 `MMFR-A2-train-integration-v3`、A+MID-A、六类 failure、severity、curriculum、`p_clean=0.25`、`lambda_rel=0.1`、500 epoch、batch size 10、optimizer、selector、evaluator、主 success gate、43 个 optimizer-missing 参数全部不变。只修改“执行顺序、授权状态和结果解释”。当前 v3 五项本地 qualification 继续有效，不需要因此建立 v4。
2. **在 protocol、current-status、open-decisions 和总规划中新增一个阶段：**  
`MMFR-A2-v3-exploratory-one-arm-screening`。  
它位于 cloud capacity probe 之后、正式 paired training 之前。其目的只回答：  
**“Depth-corruption-v3 是否表现出值得继续投入第二个 500 epoch clean control 的明显研究苗头？”**  
明确写明：这是成本筛选，不是正式因果实验，不改变原来的主 success gate。
3. **云端短 probe 仍然必须先执行。** 未经用户单独授权，不得创建实例或运行 probe。probe 仍使用冻结的 batch size 10，优先 RTX 4090；只检查显存、AMP/GradScaler、吞吐、checkpoint save/reload 和停止流程，不做正式效果判断。当前文档已经规定 probe 授权不等于正式训练授权。
4. **probe 通过后，第一阶段只申请一个正式 500 epoch 训练授权：**  
`MMFR-A2-depth-corruption-train-v3`。  
不同时训练 `MMFR-A2-clean-control-v3`。  
corruption model 必须从冻结的官方 pretrained 独立开始，不允许从 Quick-B0、v1 或 v2 checkpoint 续训。保持现有 selector：clean `val-dev`、original-full、scale 1.0、无 flip、按 mIoU 选择 checkpoint，tie-break 为 earlier epoch。
5. **第一阶段训练完成后，冻结 corruption-v3 checkpoint 后再评价。** 使用当前已经冻结的主 evaluator、evaluation seed 和 failure conditions，不得为了 screening 新设计一套容易得到漂亮结果的条件。至少输出：
  - clean；
  - 六个单 failure；
  - 三个 mixed failure；
  - mIoU 为主；
  - 同时保留 mAcc、mF1、Boundary IoU；
  - 六单 failure 的 macro-average。
  当前正式评价条件保持原样。
6. **同时使用现成 Quick-B0 checkpoint 作为历史 screening reference，不重训 Quick-B0。** 对 Quick-B0 使用能够适用的同一 `val-dev`、同一 evaluator 和同一评价代码生成结果。必须在所有表格标题中明确标记：  
`historical RGB development reference`  
而不是：  
`clean control`
  因为 Quick-B0 是 single-seed RGB development baseline，历史 clean mIoU 为 `58.79`，不是当前 v3 的配对 RGB-D clean identity。
7. **禁止把 Depth-corruption-v3 − Quick-B0 写成“MMFR robustness gain”。** 第一阶段允许的结论只有：
  - `screening-promising`
  - `screening-unpromising`
  - `screening-inconclusive`
  不得使用：
  - `A2 supported`
  - `MMFR improves robustness`
  - `causal gain`
  - `正式成功`
  原因是正式比较必须是：  
  `Depth-corruption-v3 − Clean-control-v3`。  
  当前预注册主 success gate也是基于这两个公平身份。
8. **不要让下级模型自己发明新的 screening 数值门槛。** 第一阶段训练和评价结束后，只整理结果，不自动启动第二个 500 epoch。必须生成一份 `one-arm-screening-report`，至少包含：
  - corruption-v3 selected epoch；
  - clean mIoU；
  - 六单 failure mIoU；
  - 六单 failure macro-average；
  - 三 mixed failure；
  - 相对 Quick-B0 的逐条件差值；
  - telemetry 四类统计；
  - checkpoint hash；
  - source commit/tag；
  - evaluator identity；
  - GPU、耗时、费用；
  - 明确声明 Quick-B0 不是 paired clean control。
  然后状态改为：  
  `awaiting-senior-review-of-one-arm-screening`
  **必须停下来等待高级模型/用户决定，不得自动训练 clean-control-v3。**
9. **高级审查后有两条路径。**
  - 如果第一阶段明显没有研究价值：状态改为 `one-arm-screening-not-promising`，暂停 A2，不再花第二份 500 epoch 成本，保留所有结果作为探索性负结果。
  - 如果存在值得继续验证的苗头，或结果不能排除有效性：再向用户单独申请 `MMFR-A2-clean-control-v3` 的 500 epoch 正式训练授权。
10. **第二阶段一旦获批，才训练 clean-control-v3。** 必须从同一个官方 pretrained、同一个 seed、train-dev、optimizer、LR、500 epoch、batch size、selector 独立训练，不能从 corruption-v3 checkpoint 反向构造 clean control。两个 v3 身份共同冻结项继续按现协议执行。
11. **第二阶段完成后，Quick-B0 降级为普通历史参考。** 正式 A2 结论只比较：  
`Depth-corruption-v3 − Clean-control-v3`

严格使用现有主 success gate：

- 六单失效 mIoU 宏平均差值 ≥ `+1.00 pp`；
- paired 95% percentile interval 下界 > 0；
- clean mIoU 下降 ≤ `0.50 pp`；
- 至少五个单 failure 差值非负；
- 任一单 failure 不得低于 `-1.00 pp`。

不得根据第一阶段结果修改这些条件。

1. **版本管理规则：不新建科学 protocol v4。** 如果只修改执行顺序、授权矩阵、状态机和报告模板，可以继续使用 v3 scientific identity，并单独增加 execution-plan revision。不得修改冻结的生产代码、corruption、loss、target、selector 或 evaluator。如果确实碰到科学代码修改，则立即停止并重新建立 source identity。
2. **更新准确恢复点。** 修改完成后，在状态文件中写成类似：  
`v3 scientific protocol frozen; local qualifications PASS; next sequence = cloud probe authorization -> one-arm Depth-corruption-v3 500e screening authorization -> senior screening review -> conditional clean-control-v3 500e authorization -> formal paired evaluation.`

同时明确：  
**one-arm screening 的目标是减少无价值的第二次长训练，不是降低最终论文实验的公平性要求。**

这套策略最大的价值就是把原来的：

**probe → 500e clean + 500e corruption → 才知道有没有希望**

改成：

**probe → 先花一份钱跑 corruption 500e → 看苗头 → 值得才再花第二份钱跑 clean 500e。**

但要让下级模型特别记住一句话：**第一阶段 Quick-B0 只能帮我们决定“还值不值得继续花钱”，不能帮我们证明“MMFR 有效”。**正式证明依旧需要第二阶段的 clean-v3 配对实验。