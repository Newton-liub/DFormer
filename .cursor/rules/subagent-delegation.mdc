---
description: Delegate only simple, low-risk, context-heavy work
alwaysApply: true
---

# 子代理委派边界

- 默认由主代理直接理解用户意图、作出决策、修改关键代码、核验结果并整合最终答复。
- 仅将边界清晰、难度较低、失败后容易发现和纠正，但会大量消耗搜索或上下文 token 的工作交给子代理。
- 适合委派：批量代码或文档探索、关键词与引用搜索、机械化清单整理、低风险信息汇总、可由测试或主代理快速复核的简单实现。
- 不适合委派：需求解释、架构取舍、实验结论、证据与哈希核验、云资源生命周期操作、安全敏感变更、不可逆操作、最终验收、提交与推送决策。
- 不因任务规模较大、步骤较多或可以并行就自动委派；任务必须同时满足“简单、低风险、边界清晰、耗上下文”条件。
- 主代理必须复核子代理返回的关键事实，不得把子代理输出直接当作最终结论。
- 规则中不固定子代理模型；使用用户在 Cursor 中当前配置的默认子代理模型。
