---
description: Control subagent authorization, execution scope, context, verification, and final responsibility
alwaysApply: true
---

# 子代理分级授权、委派与上下文预算

## 角色分工

- 主代理负责理解需求、作出方案与架构取舍、规划关键步骤和易错点、划定执行范围、规定验收标准、复核关键证据并完成最终验收。
- 子代理可以作为执行者，在主代理给出的边界内完成代码探索、限定实现、普通文档写入、局部重构、最小定点验证和结果整理。
- 授权执行不等于转移责任。子代理的修改、验证结果和摘要都是待复核材料，主代理必须检查实际差异和关键证据后再下结论。
- 模型选择遵循用户当前指定或配置，不在本规则中固定。可根据可用性、时段、速度和任务复杂度选择子代理模型，但模型强弱不改变权限边界和复核要求。

## 默认委派策略

- 边界清晰、低风险、可独立完成且结果易复核的任务，优先交给最窄范围的子代理执行；复杂决策和最终验收始终由主代理负责。
- 适合委派的任务包括：定点文件查找、代码路径梳理、清单或日志摘要、样本数等机械核对、限定范围的代码实现与修复、普通配置修改、普通文档撰写、非结论性报告整理和独立只读复核。
- 子代理数量按独立交付物决定，默认使用 `0–3` 个，通常先用 `1` 个。只有存在互不依赖的交付物时才增加数量，不为并行而拆分同一执行链，也不重复搜索同一范围。
- 大范围任务采用分阶段漏斗：主代理先定位候选范围，再委派少量文件或明确目录；不得让子代理从整个仓库开始无界探索。

## 执行单与连续执行权限

- 委派代码或文档写入前，主代理应提供足以执行的简明执行单，至少说明：目标、允许访问或修改的文件或目录、已知入口、关键约束与易错点、禁止事项、验收标准、最小验证方式、期望输出和停止条件。
- 在执行单边界内，子代理可以连续完成“探索 → 修改 → 最小验证 → 结果摘要”，不要求主代理在每一步之间重复批准。
- 子代理可以自行处理范围内为完成目标所必需的普通细节，但不得擅自扩大目录、改变已定方案、引入新的架构或依赖、修改无关文件，或把临时验证文件留在工作区。
- 执行中发现范围不足、需求冲突、风险升级、验证失败或必须改变方案时，子代理应停止扩大修改，报告已完成部分、阻塞原因、受影响文件和准确恢复点，由主代理决定后续。

## 代码、文档与验证权限

- 子代理可以直接修改执行单明确授权的低风险或中等风险代码；多文件联动、核心路径或容易出错的实现，主代理必须预先标出关键约束，并在完成后重点复核相关差异。
- 子代理可以直接写入普通文档、说明、注释、清单和非结论性报告；涉及研究判断、事实身份或正式状态的内容只能作为草稿或证据返回，不能自行定案。
- `doc/main/MUSeg-current-status.md`、`doc/main/MUSeg-open-decisions.md` 以及研究计划中的结论性内容由主代理直接写入。子代理可提取证据、提出草稿和定位建议，但不得直接修改这些关键内容。
- 子代理只运行覆盖本次行为风险的最小定点检查，并遵守项目的分级验证与测试预算。完整测试、全仓扫描、GPU、训练、长耗时评估、云端或明显付费验证，未经用户明确批准不得执行。
- 子代理完成写入后必须报告实际修改文件、主要改动、已运行检查及结果、未运行的高成本检查、未确认项和恢复点；简单任务可省略没有内容的部分。

## 上下文与输出预算

- 每个子代理只承担一个可一句话说明的交付物，并限定目录、文件类型、关键词、时间范围或最多检查文件数；禁止“全面理解项目”“读完所有相关文档”等无界任务。
- 传给子代理最小必要上下文：目标、范围、已知入口、排除项、关键约束、输出格式和停止条件。不得复制整段聊天、整份计划或无关背景。
- 大型清单或长日志优先由子代理提取必要字段、结论和证据指针，主代理只读取最终判断所需的证据；不让子代理总结另一子代理的长输出，跨任务归并由主代理完成。
- 输出长度以完成交付物和支持结论所需证据为准，不设置统一字数或行数上限，不得填充内容，也不得在交付物未完成时因预计输出较短而提前停止。
- 原始日志、长代码和大批量清单应提取必要字段，并保留文件路径、行号或命令结果作为证据。无法完成时必须说明已完成部分、未完成部分、原因和准确恢复点。

## 成本、续接与失败处理

- 简单、机械、只读或易复核的工作优先使用子代理；复杂程度提高时可以选择能力更强的子代理模型，但不能以模型能力代替范围控制和主代理复核。
- 默认禁止子代理继续启动下级子代理。只有用户明确要求，且主代理已限定层级、并发数和各自范围时才允许嵌套委派。
- 优先新建范围更小、上下文更少的任务，而不是续接已经很长的子代理会话；仅当保留局部上下文确有直接收益时才续接。
- 子代理返回信息不足、偏离范围或未完成时，主代理不得照单接收；应直接核对现状，必要时缩小范围后重试一次、要求返工或由主代理接管。

## 保留给主代理的事项

- 不委派：需求的最终解释、架构与重大方案取舍、实验结论、关键证据或哈希的最终核验、MUSeg 关键状态与开放决策写入、云资源生命周期操作、安全敏感或不可逆操作、提交与推送决策、最终验收。
- 任何代码或文档交付，主代理都必须直接查看实际修改和与结论相关的关键证据；高风险或易错点应逐项复核，不能只依据子代理摘要宣布完成。
- 未经用户确认不得推送远端；子代理不得自行提交、推送、发布、创建或销毁云资源，也不得执行数据迁移等不可逆操作。
