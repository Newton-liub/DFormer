---
description: Control subagent scope, context, output size, and cost
alwaysApply: true
---

# 子代理委派与上下文预算

## 默认策略

- 默认由主代理理解需求、作出决策、修改关键代码、核验证据并整合最终答复；边界清晰、低风险、可独立完成且结果易复核的简单任务，优先委派给最窄范围的子代理，不再以“主代理一次命令即可完成”作为不委派理由。
- 适合委派的简单任务包括定点文件查找、清单摘要、哈希/样本数等机械核对、单一日志统计、只读代码定位和独立复核。大型清单或长日志需要阅读时，应优先让子代理提取所需字段和结论，主代理只读取必要证据。
- 独立的只读复核可委派，例如代码审查、实现核对和测试结果归纳；子代理提供第二视角，最终判断仍由主代理作出。
- 子代理数量按任务边界和风险决定，默认使用 `0–3` 个，通常先用 `1` 个；只有存在互不依赖的独立交付物时才增加数量。一次响应默认最多启动 3 个；确实需要更多子代理时，按独立交付物分批启动，并保持每批范围清晰。用户明确指定数量时，在系统限制和任务边界允许的范围内执行。
- 不为了“并行”拆分同一条执行链，也不让多个子代理重复搜索同一范围；增加子代理必须有清晰的独立交付物，不能只是把同一工作重复分配。数量原则用于避免无效并发，不应阻止合理的任务拆分。

## 任务切分与输入预算

- 每个子代理只承担一个可一句话说明的交付物，并限定目录、文件类型、关键词、时间范围或最多检查文件数；禁止“全面理解项目”“读完所有相关文档”等无界任务。
- 选择能完成任务的最窄子代理类型和最小权限：搜索优先只读探索类型，连续命令优先 shell（命令行）类型；无需修改文件时，不给出编辑目标，也不使用通用实现型子代理。
- 传给子代理最小必要上下文：目标、范围、已知入口、排除项、输出格式和停止条件。不得复制整段聊天、整份计划或与任务无关的背景。
- 大范围工作采用分阶段漏斗：主代理先定位候选，再让子代理检查少量候选；不得让子代理从整个仓库开始探索后再自行压缩上下文。
- 若预计需要读取大量长文件，先按目录或主题拆成独立小批次；不要在一个子代理中连续“搜索→通读→实现→测试→总结”。

## 输出要求

- 不设置统一的字数或行数上限。输出长度以完成交付物和支持结论所需的证据为准。
- 简单任务可以只返回简短结论和定位信息；复杂任务应补充关键证据、未确认项和建议下一步。
- 不得为了达到字数、行数或固定格式填充内容，也不得因为预计输出较短就提前结束任务。
- 原始日志、长代码和大批量清单优先提取必要字段，并保留文件路径、行号或命令结果作为证据；必要时可以返回更多证据，不要省略支撑结论的关键信息。
- 只有在交付物已经完成，或确认无法继续时才停止。若无法完成，必须说明已完成部分、未完成部分、原因和准确恢复点。
- 默认报告可包含：结论、关键证据、未确认项和建议下一步；简单任务不必补充没有内容的部分。

## 成本与续接控制

- 简单任务优先委派给最窄范围的子代理；不再把“是否节省主对话 token、时间或调用成本”作为是否委派的门槛。子代理模型成本较低时，默认优先使用子代理承接机械性、只读性和易复核工作。
- 委派仍须遵守边界、证据要求和主代理复核责任；复杂决策、架构取舍、不可逆操作和最终验收由主代理负责。
- 默认禁止子代理继续启动下级子代理，避免不透明的递归并发和成本倍增；只有用户明确要求，且主代理已限定层级、并发数与各自范围时才允许嵌套委派。
- 不让子代理总结另一子代理的输出，也不把多个长输出交给新子代理二次汇总；跨任务归并由主代理完成。
- 优先新建“更小范围、最小上下文”的任务，而不是续接已经很长的子代理会话；仅当保留其局部上下文确有净收益时才续接。
- 子代理返回信息不足、偏离范围或未完成交付物时，主代理不直接照单接收；只提取可核验证据，必要时以更窄任务重试一次或续接原任务。

## 禁止委派与最终责任

- 不委派：需求解释、架构取舍、实验结论、证据或哈希最终核验、云资源生命周期操作、安全敏感或不可逆操作、最终验收、提交与推送决策。
- 主代理必须直接复核关键事实，不能把子代理输出直接当作最终结论；规则不固定子代理模型，使用用户当前配置的默认子代理模型。
