---
description: Keep the MUSeg current-status file authoritative across conversations
alwaysApply: true
---

# MUSeg 唯一实时入口维护规则

- 每个涉及 MUSeg 研究、代码、数据、实验、云资源、报告或发布的对话，开始工作时先读取 `doc/main/MUSeg-current-status.md`；涉及实验口径时再读取 `doc/main/MUSeg-open-decisions.md`。
- `doc/main/MUSeg-current-status.md` 是当前事实、正在进行事项、边界和恢复点的唯一实时入口。计划、交接、审计、报告和 Canvas 只承担详细证据或历史快照，不得替代它。
- 两份实时文档采用**滚动窗口**：`current-status` 聚焦最近一个大动作/当前阶段、结果、授权、阻塞、下一恢复点和少量证据入口；`open-decisions` 只列当前仍真正未决且影响下一步的选择，不复制运行状态。打开后应能在几分钟内看懂；不要在末尾无限追加阶段流水账。
- 完成新的大动作后，用新阶段重写 `current-status` 主体；上一个大动作如仍有历史价值，按现有 `MMFR/90_archive/` 事件目录归档完整旧版本或依托已有正式报告、证据、protocol 与 Git history 留存，不改写历史结果和原始证据，也不为每个小动作单独建档。大动作包括正式训练轮次、Main-Val/Quick-Val 轮次、模块关闭或进入下一阶段、较大代码/目录重构；小修复、单次启动失败、单个测试/PID/hash 更新通常只更新当前动作内部状态。
- `open-decisions` 的事项一旦已决定、执行、失效或被新方案替代，就从实时文件移出；有追溯价值时归档或指向既有记录。只保留当下需要用户/上级裁决或直接阻塞下一步的研究选择。不得把已关闭的决定、完整门禁过程或历史阶段重新写回实时文件。
- 状态文件保留供复核使用的精确术语、数值、身份和证据指针；新增或改写的重要结论同时补充一句大白话说明，解释“这件事实际意味着什么”。
- 首次出现且会影响判断的专有名词，应就地给出简短解释；术语密集时可集中增加“关键概念与判断边界”，但不得用简化表述替换严格口径。
- 新表达规则只约束后续新增或改写内容，不为统一文风而重写历史事实、原始证据或已经定型的旧结论。
- 训练开始或结束、验收结论、指标或 checkpoint 身份、official-test 状态、云实例状态、证据位置、阻塞项、下一恢复步骤、提交或发布状态发生变化时，必须在同一对话的最终答复前更新当前状态文件。
- 更新发生在最终答复之前；对话结束后代理无法继续写文件。最终答复必须明确说明当前状态文件是否更新以及更新原因。
- 对话若被中止或工作未完成，必须写入已完成部分、未完成部分、准确恢复点和已验证边界，不能只把细节留在聊天记录或临时交接中。
- 只有经过直接核验的事实才能写成结论；尚未核验的内容标为“待核验”，不得根据计划、预计时间或单一日志猜测。
- 没有持久状态变化的问答、解释或只读检查不改状态时间，避免用无事实变化的对话制造虚假进度。
- 已完成事项保留简洁结论和权威证据指针；详细过程写入日期化报告。历史原始证据不得为了匹配新口径而改写。
- 真正仍开放且影响下一步的研究选择写入 `doc/main/MUSeg-open-decisions.md`；处置后移出实时文件，必要时进入现有归档或指向正式报告。一旦处置改变当前执行边界，同时同步当前状态文件。
- 任何本地提交或远端推送前，复核当前状态与工作区一致；未经用户确认不得推送远端。
