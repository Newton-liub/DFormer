---
description: Limit verification to checks justified by the changed behavior and risk
alwaysApply: true
---

# 分级验证与测试预算

## 默认边界

- 验证服务于本次改动的具体风险，不为展示“验证充分”而扩大范围。
- 文档、Rule、Skill、报告和注释等纯文本改动，默认只做内容复核与差异检查，不运行项目测试。
- 代码改动只执行覆盖本次行为风险的最小定点检查；已有可靠静态诊断足以覆盖时，不重复运行测试。
- 不主动生成临时 `test_*.py`、一次性验证脚本或与需求无关的测试用例，也不把这些文件留在工作区。

## 需要先确认的验证

以下操作只有在用户明确要求、已批准计划明确包含，或任务存在无法用更小检查覆盖的高风险时才执行；最后一种情况先向用户说明理由并取得确认：

- 新建或大幅扩展测试文件；
- 运行完整测试套件、全仓扫描或大范围重复检查；
- 运行 GPU、训练、长耗时评估、云端或会产生明显费用的验证；
- 为了追求零 warning 而处理与本次任务无关的问题。

安全敏感、不可逆、发布或数据迁移类任务仍采用其专门审批和验收规则，本规则不降低必要保障。

## 结果报告

- 最终答复只列与交付物相关的实际检查及其结果。
- 明确说明按本规则未运行的完整测试或高成本验证及原因；未运行不得写成通过。
- 不罗列与本次改动无关的 passed、error 或 warning 数量，也不把测试通过夸大为实验、训练或真实环境已经完成。
